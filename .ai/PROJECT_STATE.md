# PROJECT_STATE.md

**Authoritative snapshot of current project state.** A new session must be able to understand the project from this file plus `TASK_BOARD.md` and `WBS.md` alone, without relying on prior conversation history. If this file ever conflicts with conversation memory, this file wins (subject only to `03_MASTER_RULES.md` § 2).

**Last Updated:** 2026-09-10 (session 9 — `PHASE-1.1`, `PHASE-1.4`, `PHASE-1.2`, `PHASE-1.3` executed under explicit human authorization, Q18; `PHASE-1.5`–`.8` remain unauthorized)
**Last Verified:** 2026-09-10 (session 9) — this session substantially advances genuine `RUNTIME_VERIFIED` coverage beyond the single file session 7 established: **89 tests across 13 zero-third-party-dependency files were actually executed** this session (domain entities, application-layer use cases with fakes, and security/authorization checks — see § 14), each category deliberately broken and confirmed to fail before being restored and re-confirmed passing. `packages/contracts`' TypeScript was also genuinely compiled (`tsc -p tsconfig.json`, exit 0), a new capability this session confirmed directly (Node/TypeScript are locally available even without npm-registry access — see Risk R9's update). Everything that depends on `fastapi`/`pydantic`/`sqlalchemy`/`alembic`/`asyncpg`/`bcrypt` (none installed — Risk R9, unchanged) remains honestly `IMPLEMENTED`/`UNVERIFIED` for actual execution, though several such files (SQLAlchemy models, the migration, the Pydantic contracts) are additionally `RUNTIME_VERIFIED` at a *structural* level via dedicated AST-based tests — see § 5.

---

## 1. Project Identity

- **Name:** Seonex — SEO Research & Strategy Copilot / SEO Decision Engine
- **Evolution stage (per `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` § 15):** Stage 1 of 5 ("Keyword Research Tool" → ... → "Living SEO Intelligence System") — `PHASE-0` (Foundation) **fully executed** (8 of 8 subtasks `DONE`); `PHASE-1` (Core Platform) has **4 of 8 subtasks `DONE`** as of this session (`.1`, `.2`, `.3`, `.4` — session 9, Q18) — the project has now genuinely entered Stage 1 territory, with real domain entities, persistence, authentication, authorization, and API contracts existing for the first time. `PHASE-1.5`–`.8` remain `BACKLOG`/`READY`-pending-their-own-flags, not authorized.
- **Repository contents:** 26 approved documents in `docs/`; this `.ai/` control system; a working repository skeleton with real application code spanning both `PHASE-0` and, now, half of `PHASE-1`. `PHASE-0`: typed configuration (`.2`), async database/migration scaffolding (`.3`), a FastAPI application with a versioned health endpoint and full error architecture (`.4`), a session/token authentication mechanism (`.5`), a Next.js frontend shell with a shared-package workspace (`.6`), structured logging/metrics/tracing (`.7`), and a test suite plus a ten-stage CI pipeline (`.8`). **`PHASE-1`, new this session:** `backend/app/domain/{user,workspace,project,membership}/` — real domain entities with invariants (`.1`, extended `.3`); `backend/app/infrastructure/database/models/` and `backend/app/infrastructure/repositories/` — SQLAlchemy models and tenant-scoped repositories for all four entities (`.1`, extended `.3`); two real Alembic migrations chaining off `PHASE-0.3`'s baseline (`.1`, `.3`); `backend/app/contracts/api/` and `packages/contracts/` — versioned request/response schemas for auth and (ahead of `PHASE-1.5`) projects, in both Python and TypeScript (`.4`); `backend/app/application/commands/auth/` — five real, dependency-injected use cases (register/login/logout/password-reset) with zero third-party imports (`.2`); `backend/app/api/routes/auth.py` and `backend/app/api/dependencies/auth.py` — the first real authenticated route and `get_current_user` dependency this project has (`.2`); `backend/app/security/{tenancy,permissions,authorization}/` — the membership/role-based access-control layer (`.3`). See § 4 for the complete file-level list. **`PHASE-1.5`–`.8` remain empty placeholders** — `apps/web/features/dashboard/`, `integrations/llm/`, `backend/app/context/` untouched this session.

## 2. Current Phase / Milestone / Module / Task / Subtask

| Field | Value |
|---|---|
| Current Phase | **`PHASE-0` — Foundation** is **fully executed**: all 8 subtasks `DONE`. **`PHASE-1` — Core Platform:** 4 of 8 subtasks `DONE` (`.1`, `.2`, `.3`, `.4` — session 9, Q18); `.5`–`.8` remain `BACKLOG`/`READY`-but-flagged, not authorized. |
| Current Milestone | N/A |
| Current Module | `backend/app/domain/{user,workspace,project,membership}/`, `backend/app/infrastructure/{database/models,repositories}/`, `backend/app/application/commands/auth/`, `backend/app/contracts/api/`, `backend/app/security/{tenancy,permissions,authorization}/`, `backend/app/api/{routes,dependencies}/auth.py`, `packages/contracts/` — all new this session. Everything from `PHASE-0` unchanged. `PHASE-1.5`–`.8`'s target modules (`apps/web/features/dashboard/`, `integrations/llm/`, `backend/app/context/`) remain untouched. |
| Current Task | `PHASE-0` — complete. `PHASE-1` — `.1`–`.4` `DONE` this session; `.5`–`.8` not started. |
| Current Subtask | None executing. `PHASE-1.5` (Project Lifecycle) is now `READY` (`PHASE-1.1` `DONE`); `PHASE-1.7` (Basic AI Runtime Foundation) remains `READY` (independent track). `PHASE-1.6` remains `BACKLOG` (blocked on `.5`); `PHASE-1.8` remains `BACKLOG` (blocked on `.7`). Both `.6` and `.7` still carry their own `Human Approval Required: Yes` flags (Observations #7, #8) unresolved since session 8. |
| Lifecycle Status | `PHASE-0.1`–`.8` all `DONE`. `PHASE-1.1`–`.4` `DONE`; `PHASE-1.5`, `.7` `READY`; `PHASE-1.6`, `.8` `BACKLOG` (see `.ai/TASK_BOARD.md`). |
| Verification Status | `PHASE-0` unchanged from session 8. `PHASE-1.1`, `.3`: `IMPLEMENTED`/`PARTIALLY_VERIFIED` — the pure-Python domain/security layers are genuinely `RUNTIME_VERIFIED`; the SQLAlchemy/Alembic layers remain `IMPLEMENTED`/`UNVERIFIED` for actual execution (Risk R9) but are structurally `RUNTIME_VERIFIED` via AST-based tests. `PHASE-1.2`: same split — application-layer use cases genuinely `RUNTIME_VERIFIED` with fakes; the FastAPI route/dependency layer `IMPLEMENTED`/`UNVERIFIED`. `PHASE-1.4`: backend Pydantic contracts `IMPLEMENTED`/`UNVERIFIED` (structurally `RUNTIME_VERIFIED`); TypeScript contracts genuinely `RUNTIME_VERIFIED` (`tsc` compiles clean). `PHASE-1.5`–`.8` remain `UNVERIFIED` — not implemented. |

## 3. Overall Project Status

**Implementation has begun.** All 26 baseline documents remain `APPROVED_AS_BASELINE_*`; none were modified this session — session 6 read `docs/` and `.ai/` only, wrote application code under `apps/`, `backend/`, `infrastructure/`, `packages/`, and root config files, and did not touch any of the 26 core documents. `BOOTSTRAP-001`/`.002` remain fully `DONE`, unchanged. This session executed `PHASE-0.1` through `.4` under explicit human authorization by name ("execute and Authorize PHASE-0.1 through PHASE-0.4" — recorded as **Q13**, § 10), the project's first authorization covering real implementation rather than documentation-derivation. A git repository now exists (initialized this session) with one commit per subtask, containing: a full monorepo skeleton (`.1`), typed/validated configuration (`.2`), async database + Alembic migration scaffolding (`.3`), and a FastAPI application with a versioned health endpoint and complete error architecture (`.4`). **A material limitation was discovered and is recorded, not glossed over:** the execution sandbox had no network access and none of the declared Python dependencies (`fastapi`, `sqlalchemy`, `alembic`, etc.) installed, and no local PostgreSQL server — confirmed by direct diagnostic commands, not assumed (§ 14). Every acceptance criterion checkable without live execution was verified (`ast.parse` syntax checks, `git check-ignore`, `grep`-based structural checks); every criterion requiring the code to actually *run* (API boots, health responds, DB reachable, migration applies/reverts) is honestly `UNVERIFIED`/`PARTIALLY_VERIFIED`, recorded as new **Risk R9** (§ 9) — not claimed as `RUNTIME_VERIFIED`. `PHASE-0.5`, `.6`, `.7` are now technically `READY` (their dependencies cleared) but remain unauthorized; `.8` remains `BACKLOG`.

**Session 7:** the human's command named all four remaining subtasks ("execute and Authorize `PHASE-0.5` through `PHASE-0.8`"), recorded as **Q14** (§ 10). `PHASE-0.6` and `PHASE-0.7` carried no additional gate beyond that authorization and had `DONE` dependencies — both were executed and committed (2 commits, `.git` reconstructed fresh this session since the uploaded archive didn't include it — see § 4). `PHASE-0.5` was not executed in that turn: it carries its own `Human Approval Required: Yes` flag (Observation #1, an unconfirmed interpretation of the Phase-0/Phase-1 authentication-scope split) that a task-ID authorization doesn't by itself resolve, per `SESSION_PROMPT.md`'s Non-Negotiables — this was surfaced back to the human rather than silently decided, mirroring the Q10/Q11 Dashboard-ownership precedent. `PHASE-0.8` was likewise not executed in that turn: its own stated dependency is `.5`–`.7` reaching `DONE`, and with `.5` still pending, that dependency was only partially satisfied.

**Session 8 (`PHASE-1` decomposition, 2026-09-09):** the human's instruction was explicit and two-part, mirroring Q12's structure exactly: "Prepare `PHASE-1` for execution by decomposing it into implementation-ready milestones/tasks... but do not implement or authorize any `PHASE-1` task yet." Recorded as **Q17** (§ 10). `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` § 18's ten "potential capabilities" for `PHASE-1` were decomposed into eight subtasks (`PHASE-1.1`–`.8`, `.ai/WBS.md` § 4B), each with dependencies, required-document citations, expected output, and acceptance criteria — following the identical `24` § 87 twelve-step Execution Planning Algorithm `PHASE-0`'s own decomposition used, stopping deliberately before step 12 ("Request authorization"). Two capabilities ("user/workspace/project model" and "persistence") were merged into a single subtask (`PHASE-1.1`) rather than split, disclosed as Observation #6 rather than silently decided — the same kind of decomposition-granularity choice `PHASE-0.3` made in session 5 by folding "database" and "migrations" together. Two subtasks were found to rest on genuine, undecided scope questions rather than settled fact, and were flagged `Human Approval Required: Yes` accordingly, mirroring the Q10/Q11 Dashboard-ownership and Obs. #1/Q15 authentication-scope precedents: `PHASE-1.6` (Dashboard Shell) — whether to build a partial dashboard now (real Project Status, honest empty states for the rest) or defer the whole capability until Phase 2–4 supply its remaining data (Observation #7); `PHASE-1.7` (Basic AI Runtime Foundation) — whether to wire a real, live LLM provider adapter now or build the interface against a mock/test-only provider until an actual agent needs a live call (Observation #8). **No file, directory, or line of application code was created, modified, or deleted this session** — verified directly by a full before/after file-listing diff (§ 14) — and no core document (`docs/*.md`) was edited; only `.ai/WBS.md`, `.ai/TASK_BOARD.md`, `.ai/PROJECT_STATE.md`, and `README.md` were touched. `PHASE-1.1` and `PHASE-1.7` are the two subtasks whose dependencies are genuinely `DONE` (Lifecycle Status `READY`); no subtask's Authorization column reads anything other than `NOT AUTHORIZED`.

**Session 7, continued:** the human's next command — "execute and Authorize `PHASE-0.5` then `PHASE-0.8`" — re-named `PHASE-0.5` specifically, immediately after Observation #1 had been surfaced and explained in full, without requesting a different scope. This was treated as the human's confirmation of the interpretation already on record (recorded as **Q15**, § 10), not silently assumed — and `PHASE-0.5` was executed: `backend/app/security/authentication/{passwords.py,tokens.py}` (bcrypt + PyJWT, both newly declared dependencies). One genuine bonus: PyJWT (2.7.0) is actually importable in this session's sandbox — the only third-party dependency this project has declared that is — so the exact encode/decode/exception-handling pattern in `tokens.py` was spot-checked against a real installed copy before being written, not merely reasoned about from documentation memory. The word "then" in the human's command was read as a sequencing instruction: `PHASE-0.8` was executed only once `PHASE-0.5` genuinely reached `DONE` (not merely authorized) — at which point `.8`'s own stated dependency was, for the first time, actually satisfied (**Q16**, § 10). `PHASE-0.8` added a three-category test suite (backend-unit, API-integration, frontend-unit, plus a fourth — contract — for genuine CI-stage content) and a ten-job CI pipeline matching `20` § 61's exact stage order. **One test file, `backend/tests/unit/test_error_hierarchy.py`, was actually executed in this sandbox** — its only dependency is the stdlib, after a minor fix removed an unnecessary `fastapi` coupling from `backend/app/api/errors/__init__.py` that nothing in the codebase actually used. All 6 of its tests passed; a deliberate break (flipping `AuthenticationError.http_status` from 401 to 200) was then introduced, confirmed to fail the suite with the correct diagnostic, and reverted, confirming the suite passes again. This is the first test in this project to reach genuine `RUNTIME_VERIFIED` status, not `IMPLEMENTED`/`UNVERIFIED` — everything else this subtask touches (the other three test files, the CI workflow itself) remains `UNVERIFIED` for the same reason as everything else this session: no network access to install `pydantic`/`fastapi`/`bcrypt`/`npm` packages (Risk R9). **All 8 `PHASE-0` subtasks are now `DONE`.**

**Session 9 (`PHASE-1.1`, `PHASE-1.4`, `PHASE-1.2`, `PHASE-1.3` execution, 2026-09-10):** the human's command named four subtasks by ID range — "execute and Authorize `PHASE-1.1` through `PHASE-1.4`" — recorded as **Q18** (§ 10), the identical Q13/Q14 task-ID-range authorization pattern. None of the four named subtasks carried a `Human Approval Required: Yes` flag (that applies only to `.6`/`.7`, neither named), so unlike Q14's `PHASE-0.5` case, no separate observation needed surfacing before proceeding — Q18 authorized all four cleanly. **A genuine mid-session self-correction:** the stated execution order was initially `.1`→`.2`→`.3`→`.4` (numeric), but before `.2` was started, `.ai/WBS.md` §4B's own text for `PHASE-1.4` was re-read and found to invoke `03_MASTER_RULES.md` § 97 ("No Implementation Before Specification") specifically so that contracts exist *before* `PHASE-1.2`'s endpoints are built against them — the numeric order would have violated the WBS's own stated intent. Corrected to `.1`→**`.4`**→`.2`→`.3` before any `PHASE-1.2` code was written (so no rework was needed), disclosed in `.ai/TASK_BOARD.md`'s notes rather than silently followed. `PHASE-1.1` (Core Domain Model & Persistence): `User`/`Workspace`/`Project` domain entities (pure Python, zero infrastructure imports — `07` §14), SQLAlchemy models, tenant-scoped repositories (`ProjectRepository.get()` requires both `workspace_id` and `project_id`, structurally, per acceptance criterion 4), and a real migration (`91ae40b03721`) chaining off `PHASE-0.3`'s baseline. `PHASE-1.4` (API Contract Layer): `SuccessEnvelope` (16 §12's success half — `PHASE-0.4`'s `ErrorResponse` reused unchanged for the error half, not reinvented), auth and (ahead of `PHASE-1.5`) project contracts in both `backend/app/contracts/api/` and `packages/contracts/` (new TypeScript package, registered in root `package.json`'s workspaces — it was missing). `PHASE-1.2` (Authentication: Real User Binding): five use cases (register/login/logout/password-reset request+confirm) built with injected `Protocol`-typed dependencies rather than hard imports of `passwords.py`/`tokens.py` (both need packages not installed in this sandbox — Risk R9 — so hard-importing them would have made the use cases themselves untestable here); `get_current_user`, the first real authenticated-route dependency this project has, closing a gap `AuthenticationError`'s own `PHASE-0.4` docstring named. Account recovery got a real, disclosed, minimal implementation: no email-delivery subsystem exists yet, so the reset endpoint always returns an identical generic message regardless of whether the account exists, and the token is logged server-side rather than returned in the response. `PHASE-1.3` (Authorization Foundation): a `Membership` domain entity (`Role`: `OWNER > ADMIN > MEMBER`) — its shape was explicitly left as "an implementation-time decision" by the session-8 decomposition, made and disclosed here as **Observation #10**; `backend/app/security/{tenancy,permissions,authorization}/` populated with the actual enforcement logic, including `require_project_access`, which checks workspace membership *before* ever looking up a project, so a non-member never triggers a cross-workspace query at all. **A second disclosed decision, Observation #11:** "Organization" (`06` §5's chain) is treated as equivalent to `Workspace` for `PHASE-1` — no separate Organization entity exists, since nothing in this project's baseline actually specifies Organization as its own capability with data or lifecycle. **A real, in-session self-correction to already-committed work:** the deliberate-break methodology (applied to every new test file this session) revealed that `Membership`'s role-validation invariant wasn't covered by any test; while adding that test, the identical gap was found in `PHASE-1.1`'s `User`/`Workspace`/`Project` (`status` field never validated on direct construction) — all three were fixed retroactively, with tests, disclosed here rather than silently patched. **89 tests across 13 zero-dependency files were genuinely executed this session** (not `ast.parse`'d) — domain entities, auth use cases with an in-memory fakes module, and authorization/tenancy/permission checks — every category deliberately broken and confirmed to fail before being reverted, including four separate break rounds against the security-sensitive `PHASE-1.3` code specifically (disabling the workspace check outright; making the role-hierarchy comparison always pass, i.e. a simulated privilege-escalation vulnerability; making a fake project repository ignore `workspace_id`, i.e. a simulated cross-tenant leak; and reordering the membership/project checks) — one of which (the reordering break) revealed a test's own name overclaimed what it verified, and the test was corrected in the same session, then re-verified against the same break. `packages/contracts`' TypeScript was genuinely compiled (`tsc -p tsconfig.json`, exit 0) — Node/TypeScript are locally available in this sandbox even without npm-registry access, a capability confirmed directly this session (Risk R9 updated). Everything requiring `fastapi`/`pydantic`/`sqlalchemy`/`alembic`/`asyncpg`/`bcrypt` remains honestly `IMPLEMENTED`/`UNVERIFIED` for actual execution — none of those packages are installed (confirmed directly, not assumed) — though the SQLAlchemy models, the migrations, and the Pydantic contracts are additionally verified *structurally* via dedicated AST-based tests proving specific properties (tenant-scoping, migration reversibility, no-credential-leak) hold in the actual source, not merely asserted in prose. **`PHASE-1.1`–`.4` are now `DONE`. `PHASE-1.5`–`.8` were not touched and remain unauthorized.**

## 4. Completed Work

**From `BOOTSTRAP-001` (prior session):**
- Full bootstrap audit of all 26 documents; human resolution of Q1–Q7; documentation repairs (Q4 deferred); persisted dependency graph; `.ai/` control system created; `BOOTSTRAP-002` decomposed (planning only).

**From `BOOTSTRAP-002.1`–`.3` (session 2, explicitly authorized by the human in-conversation):**
- `BOOTSTRAP-002.1`: Read `19_DESIGN_SYSTEM.md` §§33–49 in full; confirmed the FOUNDATION tier (§33 items only) and confirmed the FOUNDATION/SHARED boundary — nothing in §§34–49 belongs in FOUNDATION.
- `BOOTSTRAP-002.2`: Read `17_UI_UX_SPECIFICATION.md` §66 and `19` §§45–49; confirmed the SHARED tier, resolved the AI/Workflow domain-agnostic-vs-specific split per the pre-existing rule in `COMPONENT_MATRIX.md` § 1, deduplicated same-named items appearing under multiple source sections (Tooltip, Empty State, Alert, Dialog/Modal), cross-checked against `18` §9.5 as a corroborating (not primary) source.
- `BOOTSTRAP-002.3`: Read `17`'s per-workspace sections (§§10–35, §§41–42) and `18` §§8–9.2; produced the full per-workspace FEATURE-tier breakdown that `BOOTSTRAP-001` had explicitly left unpopulated.
- `.ai/COMPONENT_MATRIX.md` rewritten: §§2–4 now confirmed/derived rather than provisional; new § 6 records three non-blocking observations (Dialog/Modal naming, Dashboard's lack of a `features/` module, Topical-Map/Topic-Graph/Architecture-Tree distinctness) for future sessions.
- `.ai/TASK_BOARD.md` updated: `BOOTSTRAP-002`, `.1`, `.2`, `.3` marked `AUTHORIZED` (recording the explicit human authorization event) and `.1`–`.3` marked `DONE`.

**From `BOOTSTRAP-002.4`–`.6` (session 3, explicitly authorized by the human in-conversation — see Q9):**
- Before this session's work began, a discrepancy was found in the input files: a bundled reference archive (`Seonex-main-bootstrapped-v2.zip`) contained `.ai/TASK_BOARD.md`/`.ai/PROJECT_STATE.md` versions claiming `BOOTSTRAP-002.4`–`.6` were already complete, citing an `OWNERSHIP.md` section ("Per-Component Ownership") that did not exist in that same archive's actual `OWNERSHIP.md`, and describing `COMPONENT_MATRIX.md` changes that were not present — that file was byte-identical to the un-populated version, and its own § 5 still read "not yet populated... `BLOCKED`/`NOT AUTHORIZED`." This was flagged to the human and **not** used as a basis for any work in this session; everything below was derived fresh from the 26 source documents.
- `BOOTSTRAP-002.4`: Built a Registry Framework (`COMPONENT_MATRIX.md` § 5.0) stating tier-level defaults for all 9 schema fields, each with a full source citation (`19` §§13–20, §29–30, §92–97, §108–112, §127–133, §66; `18` §§9–12; `22` §§48–49, §76–78), then applied it to every component in §§2–4 via §§5.1–5.3's per-component tables.
- `BOOTSTRAP-002.5`: Derived per-component ownership deterministically from the tier-level rules already in `OWNERSHIP.md` (Foundation/Shared → Design System maintainers; Feature → the specific `features/<module>`, per `18` §12). Added `OWNERSHIP.md` § "Per-Component Ownership". One component group — Dashboard — has no owning `features/*` module (already flagged in `COMPONENT_MATRIX.md` § 6 during `BOOTSTRAP-002.3`); per `WBS.md` §3's "Human Approval Required: Yes, if ownership is contested," this was deliberately left open rather than assigned, and is called out in both files rather than silently resolved.
- `BOOTSTRAP-002.6`: Derived per-component testing requirements from `22_TESTING_AND_VALIDATION.md` (§48 Unit/Component/Integration/E2E tiers, §49 Frontend Contract Testing, §76 Accessibility Testing, §77 Localization Testing, §78 Visualization Testing) and `19`'s own testing sections (§§127–133), folded into the same registry as tier defaults plus per-component additions (e.g. Graph Accessibility + Visualization Testing for every graph/chart/matrix/tree FEATURE component).
- `.ai/TASK_BOARD.md` updated: `BOOTSTRAP-002.4`–`.6` marked `AUTHORIZED` and `DONE` (`.5` `PARTIALLY_VERIFIED` for the one flagged exception); `BOOTSTRAP-002` parent row now `DONE`.

**Dashboard ownership decision (session 4, explicit human decision — see Q11):**
- The human reviewed the open item from session 3 (Q10) and explicitly approved assigning Dashboard and its Dashboard-specific components (Project Status Panel, Strategic Opportunities List, Decision Queue Widget, Research Health Panel) to a new, dedicated `features/dashboard` module.
- Updated `.ai/OWNERSHIP.md` § "Per-Component Ownership": Dashboard row now reads `features/dashboard`, no exceptions remain; added a resolution note with provenance (Q11).
- Updated `.ai/COMPONENT_MATRIX.md` everywhere Dashboard's ownership was represented: § 4's Feature Module column, § 5.3's Feature Module column and Notes, § 5.4's acceptance-criteria check, and § 6 Observation #2 (marked resolved, kept for traceability rather than deleted).
- Updated `.ai/TASK_BOARD.md`: `BOOTSTRAP-002.5` and the `BOOTSTRAP-002` parent row now read fully `DONE` / `IMPLEMENTED` with no open exceptions; added a note recording the resolution.
- Updated `.ai/WBS.md`: `BOOTSTRAP-002.5`'s status in both the tree view and the task-detail table now reads `DONE` with no caveat.
- **No changes made to any of the 26 core documents.** `18` §8 (Frontend Architecture, Proposed Project Structure) explicitly frames its `features/` module list as "a conceptual baseline... [that] must be validated against implementation complexity before coding," so adding `features/dashboard` extends rather than contradicts it — no consistency correction was required or made, per the human's explicit instruction not to touch the core docs unless strictly necessary.
- **No implementation performed or authorized.** This is an ownership/architecture decision only — no `features/dashboard` directory, files, or code were created, and no `PHASE-0` work was started, per the human's explicit instructions.

**From the `PHASE-0` decomposition (session 5, explicitly authorized by the human in-conversation, scoped to decomposition only — see Q12):**
- Read `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` §§16–17 (Phase Model; Phase 0 Objective/Exit Criteria), §§29–31 (Task Decomposition, Readiness, Authorization rules), § 69 (informative implementation-sequence guidance — not binding on Phase 0 specifically); `20_PROJECT_STRUCTURE.md` §§3–4 (canonical repo layout, monorepo strategy), §§7, 24–27, 29, 32 (API/observability/frontend/database/shared-package placement), §§59, 61–62 (environment model, CI structure, repo hygiene), § 68 (minimal MVP structure caution against premature empty directories); `07_TECHNICAL_ARCHITECTURE.md` § 3 (baseline stack), §§15–20 (API architecture), §§26–30 (database), §§53–58 (auth/authorization/multi-tenancy/secrets/config); `22_TESTING_AND_VALIDATION.md` § 120 (Minimal MVP Testing Scope).
- Decomposed `PHASE-0` into eight subtasks (`PHASE-0.1` Repository Bootstrap & Control-System Persistence, `.2` Environment & Configuration Foundation, `.3` Database Foundation & Migrations, `.4` API Foundation, `.5` Authentication Foundation, `.6` Frontend Foundation, `.7` Observability Foundation, `.8` Testing Foundation & CI) — mapping 1:1 onto `24` § 17's "potential scope" bullet list, with "project control system" folded into `.1` and "environments"/"configuration" and "database"/"migrations" each combined into one subtask apiece.
- Each subtask given: dependencies, required-document citations, expected output, acceptance criteria, a Human-Approval-Required flag, complexity, and an initial Lifecycle Status derived from actual dependency-satisfaction (`24` § 30) — not assumed.
- Flagged three non-blocking observations (`.ai/WBS.md` § 4): (#1) a genuine ambiguity between `24` §§17–18 over where "authentication" belongs, surfaced rather than silently resolved, with `PHASE-0.5` marked `Human Approval Required: Yes` pending confirmation; (#2) the vector-store choice (`07` § 32) deliberately deferred past Phase 0; (#3) the Next.js framework baseline treated as settled rather than reopened.
- `.ai/WBS.md` updated: § 2 task-hierarchy tree expanded; new § 4 "Task Detail — PHASE-0 Subtasks"; § 5 "Parallelization / Sequencing Notes" extended for `PHASE-0`; stale cross-references renumbered.
- `.ai/TASK_BOARD.md` updated: `PHASE-0` parent row and eight new `PHASE-0.1`–`.8` rows added, all `NOT AUTHORIZED`; notes section records the Q12 authorization event and its explicit scope limit (decomposition, not implementation).
- **No implementation performed or authorized.** No repository, directory, file, or line of code was created anywhere in the project (checked — see § 14). No core document (`docs/`) was edited. Per the human's explicit instruction, execution stops after decomposition.

**From `PHASE-0.1`–`.4` (session 6, explicitly authorized by the human in-conversation, by name — see Q13):**
- Initialized a git repository at the project root; first commit imports the pre-existing 26 docs + `.ai/` control system as the historical baseline, before any `PHASE-0` code was written.
- **`PHASE-0.1`:** created the canonical monorepo skeleton per `docs/20_PROJECT_STRUCTURE.md` §3/§6 — `apps/{api,__init__}`, `backend/app/{api,application/{commands,queries,services,use_cases},orchestration,domain,intelligence,knowledge,contracts,infrastructure/{config,database,repositories},integrations,context,security}`, `backend/tests/`, `packages/{contracts,ui}`, `infrastructure/database/{migrations/versions,seeds,fixtures,scripts}`, top-level `integrations/`, `prompts/`, `tests/`, `scripts/`. Every layer got a real `__init__.py` with a docstring citing its architectural boundary (`docs/07_TECHNICAL_ARCHITECTURE.md` §§7-14), and every directory with no immediate content got a `README.md` explaining scope and deferral, rather than being silently empty (`20` §68). `.ai/*.md` is now committed as part of this repository. `.gitignore` extended with Python/FastAPI patterns, closing **Risk R1**. Root `README.md` and `pyproject.toml` (declaring the baseline dependency set) added.
- **`PHASE-0.2`:** `backend/app/infrastructure/config/settings.py` — three `pydantic-settings` `BaseSettings` groups (`ApplicationSettings`, `SecretSettings`, `RuntimeSettings`) matching `docs/07_TECHNICAL_ARCHITECTURE.md` §58's three categories exactly, aggregated behind `Settings`/`get_settings()`. `SecretSettings.database_url` and `.secret_key` carry no default — a missing value is designed to raise at construction time, not fail silently. `.env.example` added at repo root, documenting every variable by category.
- **`PHASE-0.3`:** `backend/app/infrastructure/database/{base,session}.py` — async SQLAlchemy engine, session factory, and a `get_db_session()` FastAPI-compatible dependency; the single point through which all DB access goes (`07` §27). `alembic.ini` (repo root) + `infrastructure/database/migrations/{env.py,script.py.mako,versions/}` — Alembic wired to the async engine via the standard `run_sync()` bridge, deriving its connection string from `SecretSettings` rather than duplicating it in `alembic.ini`. One initial migration (`7ce776d78700`) establishes the migration framework itself with no domain schema, per this subtask's own explicit scope.
- **`PHASE-0.4`:** `backend/app/main.py` (`create_app()` factory) wires a request-ID middleware, three registered exception handlers, and the `/api/v1` versioning prefix around a single `GET /api/v1/health` route (`backend/app/api/routes/health.py`) — the only route in scope. `backend/app/api/schemas/errors.py` + `backend/app/api/errors/exceptions.py` implement the error envelope from `07` §19 (`code`/`message`/`details`/`request_id`/`retryable`) exactly, with an `AppError` hierarchy covering every HTTP status in `07` §20's table (400/401/403/404/409/422/429/500/502/504). `apps/api/main.py` is the thin runnable entrypoint, importing `app` from `backend/app/main.py` without adding any domain reasoning of its own (`20` §5.1).
- Each subtask committed separately (4 commits), in strict dependency order (`.1`→`.2`→`.3`→`.4`), matching the discipline `BOOTSTRAP-002.1`–`.6` set.
- **A material sandbox limitation was found and is recorded, not silently worked around:** this session's execution environment had no network access and none of `fastapi`, `uvicorn`, `pydantic`, `pydantic-settings`, `sqlalchemy`, `alembic`, `asyncpg`, or `pytest` installed, and no local PostgreSQL server — confirmed via `pip install --dry-run` (returned "No matching distribution found") and `which psql`/`pg_ctl`/`postgres` (all empty), not assumed. Every acceptance criterion checkable without live execution was checked (see § 5); every criterion requiring actual execution (API boots, health responds, DB reachable, migration applies/reverts) is `UNVERIFIED`/`PARTIALLY_VERIFIED`, not `RUNTIME_VERIFIED` — recorded as new **Risk R9** (§ 9).
- **No core document (`docs/*.md`) was edited.** `.ai/COMPONENT_MATRIX.md` and `.ai/OWNERSHIP.md` were not touched — `PHASE-0.1`–`.4` involve no UI components and no ownership decisions.

**From `PHASE-0.6`–`.7` (session 7, explicitly authorized by the human in-conversation, by name — see Q14):**
- **`.git` was not included in the `seonext-main.zip` archive uploaded at the start of this session** — only the working tree. Rather than start a new history silently, the exact working-tree state as uploaded was committed first as a baseline commit (reconstructing `PHASE-0.1`–`.4`'s session-6 output byte-for-byte, confirmed via `diff -rq` against a fresh extraction of the same archive), before any session-7 change was added. This is recorded, not glossed over.
- **`PHASE-0.6`:** `apps/web/` — Next.js App Router shell (`app/layout.tsx`, `app/page.tsx`) matching `docs/20_PROJECT_STRUCTURE.md` §25's directory shape exactly (`app/`, `components/`, `features/`, `lib/`, `hooks/`, `stores/`, `services/`, `types/`, `styles/`, `tests/`); every not-yet-populated leaf directory carries a `README.md` explaining scope/deferral, mirroring `backend/tests/README.md`'s `PHASE-0.1` precedent. `app/page.tsx` server-fetches `PHASE-0.4`'s `/api/v1/health` as the required frontend↔API smoke test. `packages/ui/` — `package.json` + `src/index.ts` (empty export), wired into `apps/web` as an npm workspace dependency (new root `package.json`) via Next's `transpilePackages`, satisfying "empty-but-wired" without implementing any component. Root `README.md`, `apps/README.md`, and `.env.example` (new `NEXT_PUBLIC_API_URL` variable) updated accordingly.
- **`PHASE-0.7`:** `backend/app/observability/{logging,metrics,tracing,audit,events}` per `docs/20_PROJECT_STRUCTURE.md` §24. `logging/` — stdlib `logging` + a custom `JSONFormatter` (dependency-free by choice). `metrics/` — `prometheus_client`-backed (new dependency, declared in `pyproject.toml`), exposing `http_requests_total`/`http_request_duration_seconds`. `tracing/` — a lightweight contextvar-based span tracker with no external backend wired yet (documented as a deliberate placeholder, same pattern as `RuntimeSettings`'s AI-runtime placeholders). `audit/`, `events/` — reserved placeholders, same convention as `backend/app/domain`/`security`/etc. from `PHASE-0.1`. A new `backend/app/api/middleware/logging.py` records both the structured per-request log line and the metrics in one pass; `backend/app/api/routes/metrics.py` exposes an unversioned `GET /metrics` (scope decision documented in-file). Both wired into `backend/app/main.py`'s `create_app()`, alongside an in-file comment explaining the `add_middleware` ordering this depends on.
- Each subtask committed separately (2 commits, after the baseline commit above), in the order `.6` → `.7` — a choice, not a technical requirement (`.7`'s dependencies, `.1`+`.4`, don't include `.6`).
- **The same category of sandbox limitation from session 6 was re-confirmed, with one partial difference:** this session's sandbox has `node`/`npm`/`npx`/`tsc` binaries pre-installed (session 6 had none), but the network egress proxy still returned `host_not_allowed` for both `registry.npmjs.org` and PyPI — confirmed via `npm ping`, `curl -I`, and `pip install --dry-run`, not assumed. Neither `npm install` nor any Python package could be installed. `PHASE-0.6`'s TypeScript was syntax-spot-checked with the sandbox's standalone `tsc` (no project `node_modules` — module-resolution errors for `react`/`next` were expected and ignored). `PHASE-0.7`'s Python was checked via `ast.parse` (all 50 backend `.py` files) plus AST-level inspection confirming every `logger.*()` call site carries structured `extra=` fields — not merely `grep`.
- **No core document (`docs/*.md`) was edited.** One non-blocking observation was found and recorded (Observation #4, `.ai/WBS.md` § 4): `backend/app/observability/events/` (this subtask) and the domain/application/integration event bus described in `docs/20_PROJECT_STRUCTURE.md` § 50 (a separate, not-yet-created `backend/app/events/`) share the word "events" but are different concerns.
- `PHASE-0.5` and `PHASE-0.8` were named in the same human command but **not executed in that turn** — see § 3's session-7 paragraph and Q14 (§ 10) for exactly why.

**From `PHASE-0.5` (session 7, authorized as Q15 after Observation #1 was surfaced and explained):**
- `backend/app/security/authentication/{passwords.py,tokens.py}`: `bcrypt`-backed password hashing; PyJWT-backed session/token issuance with two token types (`access`, `password_reset`), each carrying an `exp` claim. Operates on raw strings/opaque subject identifiers only — no User ORM model exists or is needed (PHASE-1 territory).
- `backend/app/security/{authorization,permissions,tenancy,secrets,audit,policies}/`: reserved placeholders per `docs/20_PROJECT_STRUCTURE.md` §23's full directory shape — same convention as `PHASE-0.1`/`.7`'s other not-yet-populated layers.
- `backend/app/api/errors/exceptions.py` and `backend/app/infrastructure/config/settings.py`: two stale "not yet authorized" comments corrected now that the mechanism they referred to exists.
- `pyproject.toml`: `pyjwt`, `bcrypt` added as declared dependencies.
- **No core document (`docs/*.md`) was edited.** Non-blocking Observation #5 recorded (`.ai/WBS.md` § 4): two more "same word, different concern" pairs (`security/secrets/` vs. `SecretSettings`; `security/audit/` vs. `observability/audit/`).

**From `PHASE-0.8` (session 7, authorized as Q16 after `PHASE-0.5` genuinely reached `DONE`):**
- `backend/app/api/schemas/health.py` (new): a `HealthResponse` Pydantic model, wired as `backend/app/api/routes/health.py`'s `response_model` — gives the contract-tests category something concrete to validate.
- `backend/tests/unit/test_error_hierarchy.py` (backend-unit): zero third-party dependencies by design — **actually executed** in this session's sandbox (see § 5 for the full round trip). `backend/app/api/errors/__init__.py` was fixed in the process (an unnecessary `fastapi` import was removed — nothing in the codebase used that path).
- `backend/tests/unit/test_authentication_tokens.py` (backend-unit, the substantively important one — authentication is a `docs/22` §108 high-risk area): password/token round-trips, expiration, type-confusion and wrong-secret rejection. `UNVERIFIED` — needs `pydantic-settings`/`bcrypt`.
- `backend/tests/api/test_health_endpoint.py` (API-integration): real `TestClient` round trip through `create_app()`. `UNVERIFIED` — needs `fastapi`/`httpx`.
- `backend/tests/contract/test_response_schemas.py` (contract): validates `HealthResponse`/`ErrorResponse` directly. `UNVERIFIED` — needs `pydantic`.
- `apps/web/tests/unit/health.test.ts` (frontend-unit): Vitest, mocking `fetch` to cover `services/health.ts`'s three branches. `UNVERIFIED` — needs `npm install`.
- `apps/web/{eslint.config.mjs,.prettierrc.json}` and `package.json` script/devDependency additions: lint/format/test tooling, deliberately deferred from `PHASE-0.6`'s own package.json comment to here.
- `.github/workflows/ci.yml` (new): ten sequential, `needs:`-chained jobs implementing `docs/20_PROJECT_STRUCTURE.md` §61's exact stage order. `ai-evaluation` and `e2e` are explicit no-op jobs (documented in-file, not silently dropped). `security-tests` runs `bandit` (new dev dependency) against `backend/app`.
- `pyproject.toml`: `bandit` added as a dev dependency; `[tool.ruff]`/`[tool.mypy]` config added (deliberately lenient — neither tool has been run against real output this session).
- `tests/{fixtures,golden,evaluation,e2e}/` (new, root-level): reserved placeholders per `.ai/WBS.md` §4's named layout.
- **No core document (`docs/*.md`) was edited.** A stale line in `.ai/WBS.md`'s `PHASE-0.8` description (omitting "AI evaluation" and "security" from `20` §61's stage list) was corrected while here, since it would have misdescribed this very commit's own CI pipeline.

**From the `PHASE-1` decomposition (session 8, explicitly authorized by the human in-conversation, scoped to decomposition only — see Q17):**
- Read `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` § 18 (Phase 1 Objective, "Potential capabilities," Exit Criteria), §§29–31 (decomposition/readiness/authorization rules, re-confirmed unchanged since session 5), § 87 (Execution Planning Algorithm), §§93–98 (MVP Planning Principle, Suggested MVP Capability Chain, Critical Product Distinctions, Planning Quality Gates, Anti-Patterns); `06_DATA_ARCHITECTURE.md` §§5–8, 54–55 (Tenant/Workspace Boundary, Core Identity Model, Entity Identity, Business Model, Referential Integrity, Uniqueness); `07_TECHNICAL_ARCHITECTURE.md` §§7, 14–20, 26–30, 41–46, 53–56, 92, 106, 111 (Application Layer, Dependency Direction, API/Error Architecture, Database, LLM Architecture through Tool Calling, Auth/Authorization/Multi-Tenancy/Tenant-Isolation, Cost Attribution, Provider Migration, Mock Providers); `16_OUTPUT_CONTRACTS.md` §§4–12 (Contract Definition through Canonical Output Envelope); `17_UI_UX_SPECIFICATION.md` §§6, 10, 48, 80.3, 80.9 (Workspace/Project Model, Dashboard, Empty States, relevant Anti-Patterns); `20_PROJECT_STRUCTURE.md` §§15–16, 19, 29–30, 32 (Context/Contracts/LLM-Integration/Database-Structure/Repository-Pattern/Shared-Packages); `22_TESTING_AND_VALIDATION.md` §14, §52 (Multi-Tenant/Project Isolation Testing, Security Testing); `25_CONTEXT_MANAGEMENT.md` §§1–12, §8 (Context Management overview; Context Package); `03_MASTER_RULES.md` §§8, 29, 97, 108–109, 110, 144–145 (AI Must Not Invent Evidence; LLM Provider Independence; No Implementation Before Specification; Authentication ≠ Authorization; Never Trust Client-Supplied Identity; Audit Important Actions; Database Is Not Just Storage; Persistence Must Reflect Domain State) — section maps obtained via `grep` of each document's own numbered headers first, then targeted reads, per `.ai/SESSION_PROMPT.md`'s Context Retrieval Procedure, not a blind full read of any of the nine documents.
- Directly inspected the current repository (not just the docs) before writing anything: confirmed `backend/app/domain/`, `backend/app/contracts/`, `backend/app/context/`, `backend/app/security/{authorization,permissions,tenancy}/`, `integrations/`, and `packages/contracts/` all still hold only `__init__.py`/`README.md` deferral placeholders — several already explicitly reading "Reserved for `PHASE-1`" in their own docstrings (e.g. `backend/app/security/authorization/__init__.py`) — confirming session 7's own end-of-session state, not assumed from memory.
- Decomposed `PHASE-1` into eight subtasks (`PHASE-1.1` Core Domain Model & Persistence, `.2` Authentication: Real User Binding, `.3` Authorization Foundation, `.4` API Contract Layer, `.5` Project Lifecycle, `.6` Dashboard Shell, `.7` Basic AI Runtime Foundation, `.8` Context Foundation) — mapping `24` §18's ten "potential capabilities" onto eight subtasks via one deliberate merge (Observation #6: "user/workspace/project model" + "persistence" → `PHASE-1.1`).
- Each subtask given: dependencies, required-document citations, expected output, acceptance criteria, a Human-Approval-Required flag, complexity, and an initial Lifecycle Status derived from actual dependency-satisfaction (`24` § 30, cross-checked against `.ai/TASK_BOARD.md`'s current `PHASE-0` rows directly) — not assumed.
- Flagged four non-blocking observations (`.ai/WBS.md` § 4B): (#6) the "user/workspace/project model"/"persistence" merge, disclosed not gated; (#7) `PHASE-1.6`'s partial-dashboard-vs-defer-entirely scope question, `Human Approval Required: Yes`; (#8) `PHASE-1.7`'s live-vs-mock AI provider question, `Human Approval Required: Yes`; (#9) resource-level/tool-action-level authorization deferred out of `PHASE-1.3` as "not yet necessary" (mirroring `PHASE-0.5`'s own precedent), disclosed not gated.
- Performed a full Gate/Anti-Pattern self-check (`24` §§97–98) against the decomposition as a whole before finalizing any individual subtask block — recorded in `.ai/WBS.md` § 4B's own "Decomposition Method" subsection, not merely asserted after the fact.
- `.ai/WBS.md` updated: § 2 task-hierarchy tree expanded for `PHASE-1`; new § 4B "Task Detail — PHASE-1 Subtasks"; § 5 "Parallelization / Sequencing Notes" extended with a "Within `PHASE-1`" note describing two independent tracks (platform: `.1`→`.2`/`.4`→`.3`→`.5`→`.6`; AI: `.7`→`.8`); narrative paragraphs after the task-hierarchy tree updated to reflect `PHASE-1`'s decomposed-not-executed status.
- `.ai/TASK_BOARD.md` updated: new `PHASE-1` parent row and eight subtask rows, all `NOT AUTHORIZED`; `PHASE-1.1`/`.7` `READY`, the rest `BACKLOG`; new "Notes on This Board" entry recording the Q17 authorization event and both `Human Approval Required: Yes` flags.
- `README.md` updated: one-line status pointer extended to mention `PHASE-1`'s decomposed-not-authorized state, consistent with `.ai/PROJECT_STATE.md`.
- **No core document (`docs/*.md`) was edited.** No `apps/`, `backend/`, `packages/`, `infrastructure/`, or `integrations/` file or directory was created, modified, or deleted — confirmed by a full before/after file-listing diff across the entire repository (excluding `.git`), not merely by recollection of what was touched (§ 14).

**From `PHASE-1.1` (session 9, Q18, executed first):**
- `backend/app/domain/common/ids.py` (new): `new_id()`/`is_valid_id()` — stable UUID4 identifier generation shared by every domain entity.
- `backend/app/domain/{user,workspace,project}/entity.py` (new): `User`, `Workspace`, `Project` — frozen dataclasses with invariant validation in `__post_init__`, factory methods (`.register()`/`.create()`), and immutable mutation methods (`dataclasses.replace`). Zero imports from `infrastructure`/`api` (`07` §14). `Project` carries `06` §8's business-model attributes directly (industry, markets, products, services, etc.) rather than a separate `Business` entity — this session's decomposition text already specified this mapping.
- `backend/app/infrastructure/database/models/{user,workspace,project}.py` (new): SQLAlchemy 2.0 ORM models with `to_domain()`/`from_domain()` conversions, FK constraints (`workspaces.owner_user_id → users.id` `RESTRICT`; `projects.workspace_id → workspaces.id` `CASCADE`), and a `UniqueConstraint("workspace_id", "name")` on `projects`.
- `backend/app/infrastructure/repositories/{user,workspace,project}_repository.py` (new): return domain entities, return `None` for "not found" rather than raising. `ProjectRepository.get()` requires both `workspace_id` and `project_id` as non-optional parameters and filters on both in one query — no unscoped shortcut exists (acceptance criterion 4).
- `infrastructure/database/migrations/versions/91ae40b03721_phase1_core_domain_tables.py` (new): creates `users`/`workspaces`/`projects`; `down_revision = "7ce776d78700"` (`PHASE-0.3`'s baseline). `infrastructure/database/migrations/env.py` updated to import the new models package so `Base.metadata` actually reflects these tables.
- `backend/tests/unit/test_domain_{user,workspace,project}.py` (new, genuinely executed): 14 + 8 + 11 = 33 tests (8 and 11 after this session's retroactive status-validation fix — see the `PHASE-1.3` entry below).
- `backend/tests/unit/test_project_repository_tenant_scoping.py` (new, genuinely executed): 6 AST-based tests mechanically confirming acceptance criterion 4 holds in the actual source (required-parameter check, query-filter check, no unscoped `get_by_id` shortcut).
- `backend/tests/unit/test_migration_structural_reversibility.py` (new, genuinely executed): 4 tests (later generalized to 5 under `PHASE-1.3` — see below) confirming `upgrade()`/`downgrade()` are exactly symmetric.
- `pyproject.toml`: no new dependency needed (`sqlalchemy`/`alembic`/`asyncpg`/`pydantic` were already declared from `PHASE-0`).
- **Disclosed, non-blocking:** the "Organization"-vs-"Workspace" equivalence for `PHASE-1` (Observation #11, added under `PHASE-1.3` once the naming collision with pre-existing Observation #9 was caught and fixed — see that entry).

**From `PHASE-1.4` (session 9, Q18, executed second — before `PHASE-1.2`, per the corrected order above):**
- `backend/app/contracts/api/envelope.py` (new): `SuccessEnvelope[T]` — only `status`/`data`/`created_at`/`request_id`/`contract_id`/`contract_version` populated; the AI-agent-specific categories `16` §12 also lists (`evidence`, `confidence`, `epistemic_state`, `execution.*`, `conflicts`, `recommendations`) deliberately omitted rather than fabricated, since nothing in `Auth`/`Projects` is agent-produced output.
- `backend/app/contracts/api/auth.py` (new): request/response schemas for register/login/logout/password-reset-request/password-reset-confirm — covers every `PHASE-1.2` endpoint, written first per acceptance criterion 1. No response schema anywhere includes a `password`/`hashed_password` field.
- `backend/app/contracts/api/projects.py` (new): create/update/item/list schemas for `PHASE-1.5` (not authorized this session) — written ahead of that subtask, per the same acceptance criterion.
- `packages/contracts/` (new package): `package.json`, `tsconfig.json`, `src/{envelope,errors,auth,projects,index}.ts` — TypeScript mirrors of all of the above plus `ErrorResponse` (moved here from backend-only now that `apps/web` has a reason to consume it). Root `package.json`'s `workspaces` array was missing `packages/contracts` — added.
- `backend/tests/unit/test_auth_contracts_no_credential_leak.py` (new, genuinely executed): 3 AST-based tests confirming no response-side model exposes a credential field and every `SuccessEnvelope` subclass has a unique `contract_id`.
- `pyproject.toml`: `email-validator` added (required by `pydantic.EmailStr`, used for the first time in `auth.py`) — a small, disclosed, expected addition, same pattern as `PHASE-0.5`'s `bcrypt`/`PyJWT`.
- `packages/contracts/README.md`: documents the manual (not generated) sync strategy between the Python and TypeScript contracts, disclosed as a real limitation (no schema-generation tooling installable in this sandbox), per acceptance criterion 3.

**From `PHASE-1.2` (session 9, Q18, executed third):**
- `backend/app/application/errors.py` (new): `ApplicationError` and four subclasses (`EmailAlreadyRegisteredError`, `InvalidCredentialsError`, `AccountDisabledError`, `InvalidOrExpiredResetTokenError`) — plain exceptions, not `AppError` subclasses (dependency direction: API depends on Application, not the reverse).
- `backend/app/application/commands/auth/{register,login,logout,password_reset}.py` (new): five use cases, every dependency (repository, hasher, token issuer/decoder) `Protocol`-typed and injected — zero third-party imports, confirmed by genuinely importing and running all five with fakes.
- `backend/app/api/dependencies/auth.py` (new): `get_current_user` — the first place `decode_access_token` is wired into a real authenticated route.
- `backend/app/api/routes/auth.py` (new): the composition root — binds the five use cases to real `passwords.py`/`tokens.py` implementations and `PHASE-1.4`'s contracts; translates application-layer exceptions into `AppError` subclasses at the boundary. Wired into `backend/app/api/routes/__init__.py`'s `v1_router`.
- `backend/tests/unit/_auth_fakes.py` (new, helper, not a test module itself): `FakeUserRepository` + deterministic fake hasher/token functions.
- `backend/tests/unit/test_{register,login,logout}_user_use_case.py`, `test_password_reset_use_cases.py` (new, genuinely executed): 3 + 5 + 1 + 7 = 16 tests, including that unknown-email and wrong-password produce the *identical* `InvalidCredentialsError` (account-enumeration protection) and that a disabled account is rejected at login even with the correct password.
- **Disclosed design decision, account recovery (acceptance criterion 4):** no notification/email-delivery subsystem exists (`integrations/` is empty by design); the reset endpoint always returns the same generic message regardless of whether the account exists, and the token is logged server-side (never in the HTTP response) as an honest, minimal stand-in for real delivery — the mechanism itself works correctly end-to-end given the token.

**From `PHASE-1.3` (session 9, Q18, executed last — `PHASE-1.1` and `.2` both `DONE`):**
- `backend/app/domain/membership/entity.py` (new): `Membership` + `Role` (`OWNER`/`ADMIN`/`MEMBER`) — the membership/role table shape `.ai/WBS.md` §4B explicitly left as "an implementation-time decision." Disclosed as **Observation #10**: placed in `domain/`, not directly in `security/`, since it is a domain concept with its own invariants, structurally identical to `User`/`Workspace`/`Project`.
- **Retroactive fix to already-committed `PHASE-1.1` work:** this session's own deliberate-break check for `Membership`'s role-validation invariant revealed it wasn't covered by any test; while fixing that, the identical gap (no test, and — for `User`/`Workspace`/`Project` — no actual `isinstance` check at all) was found and fixed in all three `PHASE-1.1` entities. `backend/app/domain/{user,workspace,project}/entity.py` each gained a `status`-enum validation check in `__post_init__`; `backend/tests/unit/test_domain_{user,workspace,project}.py` each gained a corresponding test. Disclosed here, not silently patched.
- `backend/app/infrastructure/database/models/membership.py`, `backend/app/infrastructure/repositories/membership_repository.py` (new): `MembershipRepository.get_for_workspace_and_user()` requires both IDs, mirroring `ProjectRepository`'s tenant-scoping pattern exactly.
- `infrastructure/database/migrations/versions/6dca5a60eeea_phase1_3_memberships.py` (new): creates `memberships`; `down_revision = "91ae40b03721"`.
- `backend/app/security/permissions/roles.py` (new): `role_meets_minimum()` — the only role-hierarchy comparison; deliberately no per-action permission table (acceptance criterion 3's deferral, pre-existing Observation #9).
- `backend/app/security/tenancy/membership.py` (new): `get_role()`/`is_member()` membership lookups.
- `backend/app/security/authorization/{errors,workspace,project}.py` (new): `require_workspace_access()`/`require_project_access()` — the actual enforcement functions `PHASE-1.5`'s future routes will call. `require_project_access` checks workspace membership *before* any project lookup; a member requesting a nonexistent project gets a distinct `ProjectNotFoundError` (404) rather than `AuthorizationDeniedError` (403).
- **Disclosed, non-blocking: Observation #11** — "Organization" (`06` §5) is treated as equivalent to `Workspace` for `PHASE-1`; no separate Organization entity exists.
- `backend/tests/unit/test_domain_membership.py` (new, genuinely executed, 8 tests), `test_authorization_checks.py` (new, genuinely executed, 12 tests, using `backend/tests/unit/_authorization_fakes.py`) — includes the required negative test (`test_non_member_cannot_access_a_workspace_they_are_not_in`), privilege-escalation test (`test_a_member_cannot_use_a_lower_role_to_pass_a_higher_role_gate`), and cross-project-leakage test (`test_member_of_workspace_a_cannot_reach_a_project_that_belongs_to_workspace_b`) named in acceptance criterion 4.
- `backend/tests/unit/test_migration_structural_reversibility.py` **generalized** (not just extended): rewritten to check every migration file under `versions/` rather than one hardcoded path, plus a new `PHASE-1.3`-specific check that `memberships` is dropped before the tables it references.
- **No core document (`docs/*.md`) was edited** across any of the four subtasks — confirmed by `git status --short` after every commit, showing only the intended `backend/`, `infrastructure/`, `packages/`, and `.ai/` paths.

## 5. Verified Work

**From `BOOTSTRAP-001`:** grep-verified "Page Map" cleanup, `03` §2 naming the PRD, `23` footer verification-states list, retired hierarchy phrases removed from `23`/`25`.

**From `BOOTSTRAP-002.1`–`.3` (session 2):**
- Automated check confirmed zero component-name overlap between the FOUNDATION and SHARED tier tables in the rewritten `COMPONENT_MATRIX.md`.
- Manual line-by-line check confirmed no FEATURE-tier row re-lists a SHARED-tier shell verbatim; every FEATURE row that is a populated *instance* of a SHARED shell says so explicitly in its Notes cell.
- Every row in all three tiers carries at least one named source-document section citation (`19 §NN`, `17 §NN`, or `18 §N.N`), satisfying each subtask's stated acceptance criterion of full traceability.

**From `BOOTSTRAP-002.4`–`.6` (session 3):**
- Manually verified every field in every tier's registry table (`COMPONENT_MATRIX.md` §§5.1–5.3) traces to either a §5.0 tier-default citation or a component-specific citation — no blank or unsourced cell.
- Manually verified `OWNERSHIP.md`'s per-component table assigns exactly one owner per component for every row — including Dashboard, once the human's decision was applied.
- Cross-checked the bundled reference archive's claims against its own actual file contents (found them unsubstantiated — see § 4) rather than accepting a self-reported "DONE" status at face value; this is the same category of check `03_MASTER_RULES.md`'s Non-Negotiables ask for regarding this session's own claims.

**From the Dashboard ownership decision (session 4):**
- Re-ran the traceability check across all five `.ai/` files after applying the decision: `grep -n "Dashboard"` against every file, confirmed no remaining reference describes Dashboard's ownership as contested, unassigned, or route-level-only-with-no-owner. Every surviving reference either states the resolved owner (`features/dashboard`) or is a historical/traceability note explicitly labeled as resolved (`OWNERSHIP.md`'s resolution paragraph, `COMPONENT_MATRIX.md` § 6 Observation #2, `TASK_BOARD.md`'s notes, this entry).
- Confirmed `COMPONENT_MATRIX.md` §§4, 5.3, 5.4, and 6 all now agree with each other and with `OWNERSHIP.md` on Dashboard's owner — no internal contradiction between sections.
- Confirmed the one-owner-per-component acceptance criterion for `BOOTSTRAP-002.5` (`WBS.md` §3) now holds with zero exceptions.
- Confirmed no edits were made to any of the 26 core documents, and no files were created outside `.ai/`.

This is **documentation-consistency verification**, not software testing or runtime verification — there is still no runtime to test.

**From `PHASE-0.1`–`.4` (session 6) — this is the project's first *structural code* verification, still not runtime verification:**
- `PHASE-0.1`: all 4 acceptance criteria confirmed — directory layout diffed against `20` §3's shape; `.ai/*.md` confirmed present in `git status`/committed; `.gitignore` correctness confirmed with `git check-ignore -v` against 5 sample paths (Python cache/venv artifacts and `.env` correctly ignored; `.env.example` and a migration file correctly NOT ignored); `git status`/`git log` confirmed no secret or generated artifact was ever staged.
- `PHASE-0.2`: all 3 pydantic-settings groups confirmed present and mapped 1:1 onto `07` §58's categories by direct code read; confirmed no secret field has a default value (code inspection: `Field(...)` with no `default=`); `.env.example` confirmed to document all 3 categories; confirmed via `git check-ignore` that `.env.example` is tracked and `.env` is not.
- `PHASE-0.3`: confirmed via `grep` across every new file that no raw SQL string-concatenation or `.format()`-built query exists (criterion 3, fully satisfied). Migration file, `env.py`, `session.py`, `base.py` all confirmed syntactically valid Python via `ast.parse`. **NOT verified:** criteria 1-2 (DB reachable; migration applies/reverts) — no PostgreSQL server or `alembic`/`asyncpg` package was available in this session's sandbox (§ 9 Risk R9).
- `PHASE-0.4`: confirmed via direct code read that `ErrorResponse`/`ErrorBody` match `07` §19's shape field-for-field; confirmed via `grep` that all 10 `http_status` values in the `AppError` hierarchy match `07` §20's table exactly; confirmed via `grep` that all 3 exception handlers (`AppError`, `RequestValidationError`, generic `Exception`) are registered; confirmed `request_id` flows from the middleware into every error response. **NOT verified:** criteria 1-2 (API boots; `/api/v1/health` responds) — `fastapi`/`uvicorn` were not installable in this session's sandbox (Risk R9).
- All new `.py` files across `.1`–`.4` (25 files) confirmed syntactically valid via `ast.parse` in a single automated pass, cross-checked against `git diff --name-only`/`git ls-files --others` so no created file was missed.

**From `PHASE-0.6`–`.7` (session 7):**
- Confirmed via `diff -rq` (excluding `.git`) that the reconstructed baseline commit is byte-for-byte identical to a fresh extraction of the uploaded `seonext-main.zip` — no content was altered while reconstructing the missing git history.
- All 50 backend `.py` files (25 from `.1`–`.4` + 25 new/changed for `.7`) confirmed syntactically valid via a single automated `ast.parse` pass.
- Walked the AST of every `.py` file under `backend/app/` looking for `logger.<info|warning|error|debug|exception|critical>(...)` call sites and confirmed each one passes an `extra=` keyword — not a `grep` text match, an actual parse-tree check. Zero raw-string log calls exist anywhere in `backend/app/`.
- `grep`-reconfirmed zero raw-SQL string concatenation across the full `backend/` tree (not just `.3`'s original files) — no regression introduced by `.7`'s changes.
- All new JSON config files (`package.json` ×3, `tsconfig.json` ×2) confirmed to parse via `python3 json.load`.
- TypeScript syntax spot-checked against the sandbox's global `tsc` (v6.0.3) for `app/layout.tsx`, `app/page.tsx`, `services/health.ts` — no syntax errors surfaced (module-resolution errors for `react`/`next` were expected, since no `node_modules` exist, and are not evidence of a code defect).
- Confirmed via direct inspection that `backend/app/security/__init__.py`, `backend/app/api/errors/exceptions.py`, and `backend/app/infrastructure/config/settings.py` still accurately read "not yet authorized" for `PHASE-0.5` after this session's changes — no stale claim was accidentally left implying `.5` had been done.

**From `PHASE-0.5` (session 7, Q15):**
- PyJWT's exact `encode`/`decode`/exception-handling behavior was spot-checked in a throwaway script against the real installed copy (2.7.0) before `tokens.py` was written — round-trip encode→decode, an expired token raising `ExpiredSignatureError`, and a wrong-secret decode raising `InvalidTokenError`/`InvalidSignatureError` were all directly confirmed, not assumed from documentation memory.
- AST/grep-confirmed `passwords.py` never returns or logs the raw `plain_password` argument; confirmed no role/permission/authorization logic exists anywhere under `backend/app/security/` (criterion 4) via a targeted case-insensitive grep for `role|permission|is_allowed|has_access|can_access` — zero matches outside docstrings explaining what's deliberately absent.
- Confirmed `tokens.py` does not import from `backend/app/api/` (dependency-direction check, `20` §2.2).
- All Python files re-verified via `ast.parse` after these additions (59 files at that point).

**From `PHASE-0.8` (session 7, Q16) — the first genuine live-execution verification in this project:**
- `backend/tests/unit/test_error_hierarchy.py` was **actually run** with `PYTHONPATH=. python3 backend/tests/unit/test_error_hierarchy.py`: all 6 tests passed on the first run.
- Acceptance criterion 3 ("CI fails on a deliberately broken build") was then proven empirically, not just asserted: `AuthenticationError.http_status` was manually changed from `401` to `200` in `backend/app/api/errors/exceptions.py`, the same test command was re-run, and it failed with `AssertionError: AuthenticationError.http_status was 200, expected 401 per docs/07_TECHNICAL_ARCHITECTURE.md §20` — the exact, correct diagnostic. The file was then restored from a backup and `diff`-confirmed identical to the pre-break version; the test suite was re-run a third time and passed again, confirming the restoration was clean.
- Getting to a zero-dependency test required removing an eager `fastapi` import from `backend/app/api/errors/__init__.py`; before making that change, every existing import of `register_exception_handlers` in the codebase was grepped for and confirmed to already go through `.handlers` directly (only `backend/app/main.py`), so removing the package-level re-export was confirmed non-breaking before it was made, not assumed safe.
- All 64 backend `.py` files re-verified via `ast.parse` in a final pass immediately before the `PHASE-0.8` commit.
- `pyproject.toml`, `.github/workflows/ci.yml` validated via `tomllib.load`/`yaml.safe_load` respectively; all new/changed JSON files (`apps/web/package.json`, `.prettierrc.json`) validated via `json.load`.
- TypeScript syntax spot-checked (`apps/web/tests/unit/health.test.ts`, `vitest.config.ts`, `eslint.config.mjs`) with the sandbox's global `tsc` — only module-resolution errors surfaced (`vitest`, `@eslint/eslintrc`, `@types/node`'s `process` global, `import.meta.dirname`'s type — all traceable to the absent `node_modules`, none a genuine syntax defect).
- Confirmed via `git status`/`git diff --stat` before each commit that exactly the intended files were staged — no accidental inclusion of scratch/backup files (e.g. `/tmp/exceptions_backup.py`, created outside the repository specifically so it couldn't be accidentally committed).

**From the `PHASE-0` decomposition (session 5):**
- Manually verified every `PHASE-0.x` subtask cites at least one source-document section (`07`, `20`, `22`, or `23`) for both its scope and its acceptance criteria — no unsourced subtask.
- Manually re-derived the dependency chain (`.1`→`.2`→{`.3`,`.4`}→{`.5`,`.6`,`.7`}→`.8`) and confirmed it forms a valid DAG with no cycles and no forward reference to a not-yet-defined subtask.
- Cross-checked `.ai/WBS.md`, `.ai/TASK_BOARD.md`, and this file against each other after editing: all three agree that `PHASE-0.1` is the only `READY` subtask, all eight subtasks read `NOT AUTHORIZED`, and no subtask's Verification Status is anything other than `UNVERIFIED`.
- Confirmed (via directory listing before and after this session's edits) that no file or directory was created or modified anywhere in the project except the three `.ai/*.md` control files named above — in particular, no `apps/`, `backend/`, `packages/`, or `infrastructure/` directory exists.

**From the `PHASE-1` decomposition (session 8):**
- Manually verified every `PHASE-1.x` subtask cites at least one source-document section (`06`, `07`, `16`, `17`, `20`, `22`, or `25`) for both its scope and its acceptance criteria — no unsourced subtask.
- Manually re-derived the dependency graph (`.1`→{`.2`→`.3`, `.4`}→`.5`→`.6`; independently, `.7`→`.8`) and confirmed it forms a valid DAG with no cycles, no forward reference to a not-yet-defined subtask, and no dependency on anything outside already-`DONE` `PHASE-0` work or other `PHASE-1` subtasks — no accidental forward reference into `PHASE-2`+.
- Cross-checked `.ai/WBS.md`, `.ai/TASK_BOARD.md`, and this file against each other after editing: all three agree that `PHASE-1.1` and `PHASE-1.7` are the only `READY` subtasks, all eight subtasks read `NOT AUTHORIZED`, and no subtask's Verification Status is anything other than `UNVERIFIED`.
- Ran a full repository file-listing snapshot (`find . -type f`, excluding `.git`) before this session's first edit and again after its last edit; diffed the two — confirmed the only files that changed are `.ai/WBS.md`, `.ai/TASK_BOARD.md`, `.ai/PROJECT_STATE.md`, and `README.md` (§ 14 records the exact diff). No `docs/*.md` file, and no file under `apps/`, `backend/`, `packages/`, `infrastructure/`, or `integrations/`, was created, modified, or deleted.
- Confirmed, before starting: `PHASE-0`'s Lifecycle Status actually reads `DONE` for all eight subtasks in `.ai/TASK_BOARD.md` (not merely recalled from conversation memory) — `PHASE-1.1`/`.7`'s dependency claims were checked against this direct read, not assumed.
- Confirmed none of the 26 core `docs/*.md` files were opened for editing this session (only read).

**From `PHASE-1.1`–`.4` (session 9) — the largest genuine `RUNTIME_VERIFIED` expansion this project has had:**
- **Domain entities (33 tests across `User`/`Workspace`/`Project`, plus 8 for `Membership` under `.3` = 41 total):** every file imports and runs with zero third-party dependencies — confirmed by actually running `python3 -m backend.tests.unit.test_domain_*`, not just `ast.parse`. Each entity's test suite had a deliberate invariant break introduced (e.g. disabling the empty-hashed-password check, disabling role validation), confirmed to fail with the expected `AssertionError`, then reverted and re-confirmed passing.
- **`ProjectRepository` tenant-scoping (6 tests) and migration reversibility (originally 4, generalized to 5 under `.3`):** `sqlalchemy`/`alembic` are not installed, so these are AST-based structural checks, not execution — but they are themselves genuinely executed Python (stdlib `ast` module only) and were each deliberate-break-tested: making `workspace_id` optional on `ProjectRepository.get()` was confirmed to fail the corresponding test; reordering a migration's `drop_table`/`drop_index` calls was confirmed to fail the corresponding test.
- **Auth contracts (3 tests):** confirmed no response-side Pydantic model exposes a credential field, mechanically — a deliberately reintroduced `hashed_password` field on `RegisterData` was confirmed to fail the check.
- **TypeScript contracts:** `tsc -p packages/contracts/tsconfig.json` compiles clean (exit 0). A throwaway consuming file with two deliberate type errors (`number` where `string` expected; a required field omitted) was confirmed to produce real `tsc` diagnostics (`TS2322`, `TS2741`), then removed — proving this is a genuine compiler check, not a rubber stamp. This is a new capability this session confirmed directly: Node/TypeScript (`v22.22.2`/`6.0.3`) are locally available in this sandbox with zero npm-registry access needed for dependency-free `.ts` files (Risk R9 updated accordingly).
- **Auth use cases (16 tests: register 3, login 5, logout 1, password-reset 7):** all five use cases (`backend/app/application/commands/auth/*.py`) genuinely imported and executed against `backend/tests/unit/_auth_fakes.py`'s in-memory fakes — no `bcrypt`/`sqlalchemy`/`pydantic-settings` needed, by design (`Protocol`-typed injected dependencies). Three separate deliberate breaks were introduced and confirmed to fail the corresponding suite, then reverted: disabling `register_user`'s duplicate-email check; disabling `login_user`'s password verification entirely; dropping `confirm_password_reset`'s disabled-account check.
- **Authorization checks (12 tests):** genuinely executed against `backend/tests/unit/_authorization_fakes.py`. **Four separate deliberate-break rounds**, each confirmed to fail the corresponding test then reverted: (1) disabling `require_workspace_access`'s check entirely — caught immediately by the negative test; (2) making `role_meets_minimum` always return `True` (a simulated privilege-escalation vulnerability) — caught by the role-hierarchy unit test; (3) making a fake project repository ignore `workspace_id` (a simulated cross-tenant data leak) — caught by the cross-project-leakage test; (4) reordering `require_project_access`'s membership/project checks — **this break was initially not caught**, revealing that `test_non_member_is_rejected_before_any_project_lookup_even_for_a_real_project_id`'s scenario (a real project that legitimately existed in the workspace being probed) produced the same final exception regardless of check order, so the test didn't actually verify what its name claimed. The test was rewritten in the same session to use a project that exists in a *different* workspace (so the two orderings produce genuinely different exceptions), then re-verified against the identical break — this time correctly caught with a diagnostic message naming the exact ordering bug.
- Full regression: all 89 tests (13 files) re-run together after every subtask's changes, confirming no earlier test broke; `python3 -m compileall` across the entire `backend/` and `infrastructure/` trees confirmed zero syntax errors at each commit point.
- Confirmed, before starting `PHASE-1.2`: `.ai/WBS.md` §4B's `PHASE-1.4` text was re-read and found to require contracts precede `PHASE-1.2`'s endpoints (`03` §97) — the execution order was corrected from the originally-stated `.1`→`.2`→`.3`→`.4` to `.1`→`.4`→`.2`→`.3` *before* any `PHASE-1.2` code was written, not after.
- Confirmed via direct sandbox diagnostics (not assumed from session 7's notes): `python3 -c "import sqlalchemy"` etc. fail with `ModuleNotFoundError` for every `PHASE-1`-relevant package except `jwt` (PyJWT, consistent with session 7); `pip install --dry-run sqlalchemy` and `curl -I https://pypi.org` both confirm no network egress (`x-deny-reason: host_not_allowed`).

## 6. Unverified / Not Applicable Work

**Nothing has reached `TESTED` or `VALIDATED` status, and only one file has reached `RUNTIME_VERIFIED`.** All 8 `PHASE-0` subtasks are `IMPLEMENTED` — "written and structurally/statically checked" — but for most of them that's still short of "run." The one exception: `backend/tests/unit/test_error_hierarchy.py` was genuinely executed, including a deliberately-broken-build round trip (§ 5, § 14). Specifically still unverified:
- Whether the FastAPI app actually boots (`PHASE-0.4` criterion 1) — never executed; `fastapi`/`uvicorn` are not installed in this session's sandbox.
- Whether `/api/v1/health` actually responds over a real socket (`PHASE-0.4` criterion 2) — same reason. (`test_health_endpoint.py` exercises this via `TestClient`, which is written correctly but has itself never run.)
- Whether the database is actually reachable (`PHASE-0.3` criterion 1) — no PostgreSQL server was available.
- Whether the initial migration actually applies and reverts (`PHASE-0.3` criterion 2) — `alembic`/`asyncpg` are not installed, and there is no database to run it against.
- Whether `pydantic-settings` actually validates as designed at construction time (`PHASE-0.2` criterion 2) — `pydantic`/`pydantic-settings` are not installed.
- Whether `apps/web` actually runs locally and renders (`PHASE-0.6` criterion 1) — `npm install`/`next dev`/`next build` never ran; no network access to the npm registry.
- Whether the frontend's health-check fetch actually succeeds against a running backend (`PHASE-0.6` criterion 3) — neither side of that request has ever executed together, though `test_health_endpoint.py` and `health.test.ts` each independently exercise their own side.
- Whether `prometheus_client`'s actual installed API matches how it's used in `backend/app/observability/metrics/registry.py` (`PHASE-0.7` criterion 2) — the package was never installed or import-tested.
- Whether `bcrypt`'s actual installed API matches `passwords.py`'s usage (`PHASE-0.5` criterion 1) — never installed or import-tested. (PyJWT's usage in `tokens.py`, by contrast, *was* spot-checked against a real installed copy — § 5.)
- `test_authentication_tokens.py`, `test_health_endpoint.py`, `test_response_schemas.py`, `health.test.ts` — all written, all syntax-valid, none executed (need `pydantic`/`fastapi`/`bcrypt`/`npm install` respectively).
- The CI workflow itself (`.github/workflows/ci.yml`) has never run against a real GitHub Actions runner.

See Risk R9 (§ 9) for what a future session needs (package install + local Postgres + `npm install`) to close this gap, and `.ai/TASK_BOARD.md`'s Verification Status column for the precise `IMPLEMENTED`/`PARTIALLY_VERIFIED`/`UNVERIFIED` split per subtask.

`BOOTSTRAP-002.1`–`.6` and the Dashboard ownership decision remain documentation/decision artifacts, not code — `features/dashboard` is still only an ownership assignment; no such directory exists under `apps/web/features/` yet (that directory's own `README.md` explains why). `PHASE-0` itself is fully authorized and executed — there is no remaining unauthorized `PHASE-0` work.

**`PHASE-1` is a planning artifact only for `.5`–`.8` — same category as the `PHASE-0` decomposition (session 5) or the Dashboard ownership decision (session 4), not yet implemented.** `PHASE-1.5`–`.8` carry Verification Status `UNVERIFIED` for that reason — there is no code yet to have a verification outcome one way or the other.

**`PHASE-1.1`–`.4` (session 9) are implemented, with a now-familiar split: genuinely `RUNTIME_VERIFIED` where the code has zero third-party dependencies (pure-Python domain entities, `Protocol`-injected use cases, AST-based structural checks, and — new this session — `tsc`-compiled TypeScript), and `IMPLEMENTED`/`UNVERIFIED` wherever `fastapi`/`pydantic`/`sqlalchemy`/`alembic`/`asyncpg`/`bcrypt` are required to actually execute the code (none installed — Risk R9, unchanged from `PHASE-0`).** Specifically still unverified for `PHASE-1.1`–`.4`:
- Whether the SQLAlchemy models (`backend/app/infrastructure/database/models/*.py`) actually map correctly against a real Postgres schema — never executed; `sqlalchemy`/`asyncpg` are not installed.
- Whether the two new migrations (`91ae40b03721`, `6dca5a60eeea`) actually apply and revert against a real database — `alembic` is not installed and no Postgres server is reachable; only *structural* reversibility (symmetric `upgrade()`/`downgrade()`) was verified, via AST inspection, not a real `alembic upgrade head`/`downgrade -1`.
- Whether the repositories (`UserRepository`, `WorkspaceRepository`, `ProjectRepository`, `MembershipRepository`) actually round-trip a domain entity through a real database session — never executed.
- Whether `backend/app/contracts/api/*.py`'s Pydantic schemas actually validate/serialize as designed — `pydantic` is not installed; only `ast.parse` and the AST-based credential-leak/contract-ID-uniqueness tests were run, not real `BaseModel` construction.
- Whether `backend/app/api/routes/auth.py` and `backend/app/api/dependencies/auth.py` actually work as FastAPI routes/dependencies (request parsing, `Depends()` resolution, response serialization) — `fastapi` is not installed; never executed as a real HTTP request.
- Whether `passwords.py`'s `bcrypt`-backed hashing and `tokens.py`'s `pydantic-settings`-dependent `get_settings()` call actually work when wired into the real `register`/`login` routes end-to-end — `bcrypt`/`pydantic-settings` are not installed. (The *use-case orchestration logic* that calls them, by contrast, *was* genuinely tested — with fakes standing in for exactly these two dependencies.)
- Whether `packages/contracts`' TypeScript types actually agree with the Pydantic schemas' real runtime behavior (as opposed to their declared shape, which was manually cross-checked) — no way to verify semantic agreement without both sides running against the same real HTTP traffic.

See Risk R9 (§ 9) for what a future session needs (package install + local Postgres) to close this gap.

## 7. Current Objective

**`PHASE-0` is complete; `PHASE-1.1`–`.4` are now done (session 9, Q18); `PHASE-1.5`–`.8` remain undecided.** All 8 `PHASE-0` subtasks are `DONE`/`AUTHORIZED` (Q13–Q16). `PHASE-1.1`–`.4` are `DONE`/`AUTHORIZED` (Q18). `PHASE-1.5` (Project Lifecycle) is now `READY` (`PHASE-1.1` `DONE`) but **not authorized**. `PHASE-1.7` (Basic AI Runtime Foundation) remains `READY` but unauthorized, still gated by Observation #8. `PHASE-1.6` (Dashboard Shell) and `PHASE-1.8` (Context Foundation) remain `BACKLOG`, blocked on `.5` and `.7` respectively. The project's objective now is one of:
(a) a human decision to authorize `PHASE-1.5` — the natural next step now that `.1`–`.4` are done and `.5`'s only dependency (`.1`) is satisfied — following the same pattern Q18 itself used;
(b) a decision on Observations #7/#8 (`.ai/WBS.md` § 4B) before authorizing `PHASE-1.6`/`.7` specifically — unchanged since session 8, neither subtask was touched this session;
(c) a session with package-install, local-database, and `npm`-registry access to close Risk R9 by actually running every `PHASE-0` **and now `PHASE-1.1`–`.4`** live-execution criterion against the code already written — doesn't require new authorization, since all of it is already `AUTHORIZED`/`DONE`; or
(d) further decomposition/re-scoping of anything in `PHASE-1.5`–`.8`, if the human wants to revisit session 8's own decomposition before authorizing further.

No `PHASE-1.5`–`.8` implementation is authorized. Nothing in this session should be read as authorization to proceed past `PHASE-1.4` without an explicit instruction to do so.

## 8. Current Blockers

**None for `PHASE-0` — it is entirely `DONE`.** The only open item is **verification**, not completion: `PHASE-0.2`–`.8`'s live-execution criteria (does the app boot, does the DB migrate, does the frontend render, does `bcrypt`/`prometheus_client` behave as assumed, does the CI workflow actually run) remain unconfirmed because no session's sandbox has ever had network access to install any third-party package (Python or npm) or a local PostgreSQL server — confirmed by direct diagnostic commands across sessions 6, 7, and 9, not assumed. This does not block moving further into `PHASE-1`; it blocks upgrading any subtask's Verification Status to full `RUNTIME_VERIFIED` for its infrastructure-dependent parts.

**`PHASE-1.1`–`.4` are `DONE` — the same non-blocking verification gap applies to their SQLAlchemy/Alembic/FastAPI/Pydantic-dependent parts, unchanged in kind from `PHASE-0`'s.** See § 6 and Risk R9.

**`PHASE-1.5` is `READY` and unauthorized — a candidate for the human's next authorization, not blocked on anything technical.** `PHASE-1.6` (Observation #7) and `PHASE-1.7` (Observation #8) still carry their own non-technical, non-blocking-but-unresolved scope flags, unchanged since session 8 — neither was touched this session. `PHASE-1.8` remains blocked on `PHASE-1.7` reaching `DONE`.

## 9. Known Risks

| ID | Risk | Status |
|---|---|---|
| R1 | Repo `.gitignore` covers only Node/TypeScript/Next.js/Vercel; `07_TECHNICAL_ARCHITECTURE.md` § 3 specifies a Python/FastAPI backend. | **Resolved 2026-09-07** (session 6, `PHASE-0.1`) — `.gitignore` extended with Python/FastAPI patterns (`__pycache__/`, `.venv/`, `.pytest_cache/`, etc.), verified via `git check-ignore` against sample paths. |
| R2 | Dependency-metadata convention is inconsistent across docs 07–13 (cumulative vs. direct); `13_AGENT_SPECIFICATIONS.md` omits 09/10/11/12 despite needing them. | Deferred per Q4 → tracked as `DOCS-MAINT-001` in `.ai/WBS.md` |
| R3 | Bootstrap edits were verified by targeted grep, not a full manual re-read of every affected document in a rendered viewer. | Open — recommend a human skim before next session |
| R4 | `19` §39/`18` §9.5 name a component "Dialog"; `17` §66 names the same concept "Modal." Low-severity naming duplication, not a functional issue. | Noted in `COMPONENT_MATRIX.md` § 6; candidate for `DOCS-MAINT-001` or a future terminology pass. Not blocking. |
| R5 | Dashboard's components (`COMPONENT_MATRIX.md` § 4) didn't map to any `features/` module in `18` §8 — Dashboard was a route-level composition, not a feature module. | **Resolved 2026-09-06** by explicit human decision (Q11): a new `features/dashboard` module is Dashboard's owner. Decision only — module not scaffolded. |
| R6 | A bundled reference archive (`Seonex-main-bootstrapped-v2.zip`) included with session 3's input files contained `.ai/TASK_BOARD.md`/`.ai/PROJECT_STATE.md` claiming `BOOTSTRAP-002.4`–`.6` were already done, with citations that didn't match that archive's own actual file contents. | Identified and flagged to the human before any session-3 work began; not used as a basis for this session's output (see § 4). No corrective action needed beyond this record — the archive itself was not modified. |
| R7 | `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` §§17–18 list "authentication" under both `PHASE-0` ("authentication foundation") and `PHASE-1` ("authentication," alongside "authorization" and the user/workspace/project model), without stating how the two relate. | **Resolved 2026-09-08** (session 7, Q15) — this session's decomposition's reading (Phase 0 = bare mechanism, Phase 1 = the model built on top; recorded as Observation #1, `.ai/WBS.md` § 4) was surfaced to the human and confirmed when they re-authorized `PHASE-0.5` immediately after the flag was explained, without requesting a different scope. `PHASE-0.5` was executed under this interpretation. |
| R8 | `07_TECHNICAL_ARCHITECTURE.md` § 32 leaves the semantic-retrieval vector store open (pgvector vs. a dedicated store such as Qdrant). | Not a `PHASE-0` blocker — no semantic retrieval exists at the foundation stage. `PHASE-0.3` deliberately excludes this choice (Observation #2, `.ai/WBS.md` § 4); tracked here so it isn't lost before Phase 2/3 needs it. |
| R10 | `PHASE-1.6` (Dashboard Shell): `17_UI_UX_SPECIFICATION.md` § 10 describes four dashboard sections, but three (Strategic Opportunities, Decision Queue, Research Health) depend on Topic/Decision/Evidence/SERP data that doesn't exist until `PHASE-2`–`4`. This session's decomposition proposes building the fourth (Project Status) against real data now and showing honest empty states for the rest, rather than deferring the whole capability — a scope choice, not a settled fact. | **Open — mirrors Observation #7 (`.ai/WBS.md` § 4B).** Flagged for human confirmation before `PHASE-1.6` specifically is authorized; does not block `PHASE-1.1`–`.5`, `.7`, `.8`. |
| R11 | `PHASE-1.7` (Basic AI Runtime Foundation): `07_TECHNICAL_ARCHITECTURE.md` §§41–46 specify the runtime abstraction but not whether a real, live LLM provider must be wired at `PHASE-1`, when no agent yet exists to call it (`24` § 94's capability chain places agents in `PHASE-2`+). Wiring a live provider means a new external dependency, a real secret, and real cost exposure (`07` §§92, 106); building against a mock/test-only provider (`07` § 111) defers all of that until an agent actually needs it. | **Open — mirrors Observation #8 (`.ai/WBS.md` § 4B).** Flagged for human confirmation before `PHASE-1.7` specifically is authorized; does not block `PHASE-1.1`–`.6`, `.8`. |
| R9 | `PHASE-0.3`/`.4`'s live-execution acceptance criteria (DB reachable; migration applies/reverts; API boots; health endpoint responds) could not be runtime-verified in session 6's sandbox: no network access (`pip install --dry-run` confirmed "No matching distribution found" for `fastapi`), none of `fastapi`/`uvicorn`/`pydantic`/`sqlalchemy`/`alembic`/`asyncpg`/`pytest` pre-installed, and no local PostgreSQL server (`which psql`/`pg_ctl`/`postgres` all empty). Confirmed by direct diagnostic commands, not assumed. | **Open, re-confirmed session 9 across `PHASE-1.1`–`.4` as well, with one new exception found.** Session 9's sandbox (fresh, not assumed to match session 7's): `fastapi`/`pydantic`/`pydantic_settings`/`sqlalchemy`/`asyncpg`/`alembic`/`bcrypt`/`prometheus_client`/`pytest` all confirmed absent via direct `import` attempts; `pip install --dry-run sqlalchemy` and `curl -I https://pypi.org` both confirm no network egress (`x-deny-reason: host_not_allowed`). PyJWT (2.7.0) remains the one importable third-party package, as in session 7. **New this session: Node/TypeScript are genuinely usable** — `node` (v22.22.2), `npm`, and a global `tsc` (v6.0.3) are present, and `packages/contracts`'s TypeScript (which has zero external dependencies beyond the TypeScript compiler itself) was actually compiled — `tsc -p packages/contracts/tsconfig.json` exits 0, and was confirmed to genuinely catch injected type errors via a throwaway consuming file. This does not help `apps/web` itself (which needs `react`/`next`/etc. from the npm registry, still unreachable) but does mean any *future* zero-npm-dependency TypeScript in this repo can be genuinely compile-checked, not just `tsc`-syntax-spot-checked as sessions 6–7 did. **`PHASE-1.1`–`.4`'s pure-Python layers (domain entities, `Protocol`-injected use cases, AST-based structural tests) were also, for the first time this project, genuinely executed at scale: 89 tests across 13 files, all passing, each category deliberate-break-tested** — see § 5, § 14. Everything requiring `fastapi`/`sqlalchemy`/`pydantic`/`alembic`/`asyncpg`/`bcrypt`/`pydantic-settings` to actually run remains `IMPLEMENTED`/`UNVERIFIED`, honestly, not `RUNTIME_VERIFIED`. A future session with package-install, local-Postgres, and npm-registry access should, in addition to the 8 items session 7 already listed: (9) run `alembic upgrade head` covering both new `PHASE-1` migrations (`91ae40b03721`, `6dca5a60eeea`) and `alembic downgrade base`, confirming real reversibility beyond this session's structural AST check; (10) exercise `POST /api/v1/auth/{register,login,logout,password-reset/request,password-reset/confirm}` against a real running app + Postgres, confirming the routes/dependencies this session could only `ast.parse`; (11) run `npm install --workspace=packages/contracts` and confirm `apps/web` can actually import `@seonex/contracts`. Not a blocker to further `PHASE-1` authorization — the code is structurally sound and fully authorized/`DONE` for `.1`–`.4`; only live-execution confirmation is outstanding. |
| R12 | `PHASE-1.2`'s account-recovery flow (acceptance criterion 4) has no real email/notification delivery mechanism — `integrations/` is empty by design, and no phase has yet introduced one. The reset token is currently only logged server-side. | **Open, disclosed, not blocking `PHASE-1.2`'s own `DONE` status** (the mechanism works correctly end-to-end given the token; only *delivery* is missing, and delivery was never in `PHASE-1.2`'s acceptance criteria). Should be closed by whichever future phase introduces a notification/email subsystem — at that point, `backend/app/api/routes/auth.py`'s `password_reset_request` handler needs one line changed (call the new delivery mechanism instead of `logger.info`), not a redesign. |
| R13 | `packages/contracts` and `backend/app/contracts/api` are kept in sync by hand, field-for-field, with no automated enforcement (no OpenAPI-to-TypeScript generation tooling is installable in this sandbox — Risk R9). A future edit to one side that isn't mirrored to the other would not be caught by any test in this repository today. | Open, disclosed in `packages/contracts/README.md` and here rather than hidden. Not a `PHASE-1.4` blocker (both sides were written together, by the same session, and are confirmed to agree as of this commit) but a real ongoing risk for future sessions. Consider wiring an OpenAPI-schema-diff check into CI once `fastapi`/typescript codegen tooling is installable. |

## 10. Active / Recent Decisions (Q1–Q7 resolved 2026-09-06; Q8 session 2; Q9–Q10 session 3; Q11 session 4; Q12 session 5; Q13 session 6; Q14–Q16 session 7; Q17 session 8; Q18 session 9)

| # | Decision | Applied to |
|---|---|---|
| Q1 | Canonical authority hierarchy consolidated into `03_MASTER_RULES.md` § 2 (13-level, explicitly naming the PRD). `23` § 4 and `25` § 6 now reference it instead of restating competing versions. | 03, 23, 25 |
| Q2 | Two orthogonal task fields adopted: **Lifecycle Status** (`23` § 33 — BACKLOG/READY/AUTHORIZED/IN_PROGRESS/BLOCKED/IN_REVIEW/TESTING/VALIDATING/DONE/REJECTED/CANCELLED) and **Verification Status** (`26` § 106's 7-value model — IMPLEMENTED/TESTED/VALIDATED/RUNTIME_VERIFIED/PARTIALLY_VERIFIED/UNVERIFIED/BLOCKED). `23` § 8 and footer YAML amended to match; never conflate the two. | 23 |
| Q3 | "Page Map" retired as an ambiguous term; replaced throughout with **Page Candidate Mapping** (validated topics/intents → candidate pages), kept distinct from **Page Architecture** (single-page structure, `12`/`13`) and **Information Architecture** (site nav/content structure, `01` § 18). No fourth artifact introduced. | 01, 20, 24 |
| Q4 | Dependency-metadata standardization **deferred**; recorded as `DOCS-MAINT-001`. | (deferred, no doc changed) |
| Q5 | PRD and Product Vision are co-equal Product Layer authorities with different scopes: Vision → strategic direction; PRD → scope/requirements/MVP boundaries. Unresolved conflicts escalate to human. Folded into the Q1 edit. | 03 |
| Q6 | Operational FOUNDATION/SHARED/FEATURE taxonomy adopted for `.ai/COMPONENT_MATRIX.md`, explicitly documented as a distinct dimension from — not a replacement for — the taxonomies in `17`, `18`, `19`. Each now carries a cross-reference note. | 17, 18, 19, COMPONENT_MATRIX.md |
| Q7 | Accessibility baseline pinned to **WCAG 2.2 AA**. | 03 § 79 |
| Q8 | Human gave explicit, in-conversation authorization for `BOOTSTRAP-002.1` through `.3` by name (2026-09-06, session 2). Per `03_MASTER_RULES.md` § 2, explicit human decision is level 1 of the authority hierarchy, so this authorization is recorded on `.ai/TASK_BOARD.md` even though it arrived outside the Task Board's own edit flow. Scope is exactly those three subtasks — no more. | TASK_BOARD.md |
| Q9 | Human gave explicit, in-conversation authorization for `BOOTSTRAP-002.4` through `.6` by name (2026-09-06, session 3), following the identical Q8 pattern. Technical dependencies were already satisfied, so this was the only remaining gate. Scope is exactly those three subtasks. | TASK_BOARD.md |
| Q10 | Dashboard's ownership (R5) is a genuine contested-ownership case under `WBS.md` §3's "Human Approval Required: Yes, if ownership is contested" clause for `BOOTSTRAP-002.5`. Rather than guess, it was left explicitly unassigned and surfaced to the human (session 3). **Resolved by Q11** (session 4). | COMPONENT_MATRIX.md § 6, OWNERSHIP.md |
| Q11 | Human explicitly approved (session 4, 2026-09-06) assigning Dashboard and its Dashboard-specific components (Project Status Panel, Strategic Opportunities List, Decision Queue Widget, Research Health Panel) to a new, dedicated `features/dashboard` module — resolving Q10/R5. Stated as "the authoritative ownership decision for `BOOTSTRAP-002.5`." Decision only: `features/dashboard` has not been scaffolded, and the human explicitly instructed not to start implementation or `PHASE-0`. No core document required correction — `18` §8's module list is explicitly a non-binding "conceptual baseline." | OWNERSHIP.md § "Per-Component Ownership", COMPONENT_MATRIX.md §§4, 5.3, 5.4, 6 |
| Q12 | Human explicitly authorized `PHASE-0` decomposition and explicitly withheld authorization for implementation, in one instruction (session 5, 2026-09-06: "Authorize PHASE-0 decomposition. Do not authorize implementation."). Per `03_MASTER_RULES.md` § 2, this is a level-1 explicit human decision, scoped precisely: produce the WBS-level breakdown of `PHASE-0` into subtasks with dependencies/required-docs/acceptance-criteria; no code, no scaffolding, no directory/file creation, no subtask granted execution authorization. | WBS.md § 4 (new), TASK_BOARD.md (`PHASE-0`, `PHASE-0.1`–`.8` rows and notes), PROJECT_STATE.md §§2–3, 4–9, 12, 14 |
| Q13 | Human explicitly authorized `PHASE-0.1` through `PHASE-0.4` by name, in-conversation (session 6, 2026-09-07: "execute and Authorize PHASE-0.1 through PHASE-0.4"), following the identical Q8/Q9 pattern. Technical dependencies were satisfied in sequence as each prior subtask completed, so authorization was the only remaining gate. Scope is exactly those four subtasks — `.5`–`.8` remain unauthorized even though `.5`–`.7`'s dependencies are now `DONE`. This is the project's first authorization covering real implementation. | TASK_BOARD.md (`PHASE-0`, `.1`–`.7` rows and notes), WBS.md § 2, § 4 (`.1`–`.7` status lines), PROJECT_STATE.md §§1–14, and the repository itself (git-initialized, 4 subtask commits) |
| Q14 | Human's command named all four remaining `PHASE-0` subtasks in one instruction (session 7, 2026-09-08: "execute and Authorize `PHASE-0.5` through `PHASE-0.8`"). Per `03_MASTER_RULES.md` § 2, this is a level-1 explicit human decision — but it is not, by itself, confirmation of a *separate*, lower-level flag `PHASE-0.5` already carried (Observation #1, `Human Approval Required: Yes`) before this command arrived. Applied as: `PHASE-0.6`/`.7` (no such flag, `DONE` dependencies) — executed and committed in full. `PHASE-0.5` — not executed in this turn; the task-ID naming is authorization to schedule it, not confirmation of *which* auth-scope interpretation to build, which is what Obs. #1 specifically asks for; surfaced back to the human rather than resolved silently, mirroring the Q10/Q11 Dashboard-ownership precedent (a per-subtask flag surviving a broader authorization event). `PHASE-0.8` — not executed in this turn; its own stated dependency (`.5`–`.7` reaching `DONE`) was only partially satisfied with `.5` still pending, a technical/sequencing gate distinct from the authorization gate, identical in kind to `BOOTSTRAP-002.2`/`.3` remaining `BLOCKED` after authorization in session 2. | TASK_BOARD.md (`PHASE-0`, `.5`–`.8` rows and notes), WBS.md § 4 (`.6`–`.8` status lines, header), § 5 (session-7 sequencing note), PROJECT_STATE.md §§1–14, and the repository itself (2 new subtask commits on top of a reconstructed baseline) |
| Q15 | Immediately after Observation #1 was surfaced (Q14) and its substance explained in full — the Phase-0/Phase-1 authentication-scope interpretation, spelled out concretely — the human's next command re-named `PHASE-0.5` specifically ("execute and Authorize `PHASE-0.5` then `PHASE-0.8`"), without requesting a different scope or asking a follow-up question. Per `SESSION_PROMPT.md`'s Non-Negotiables, silently picking an interpretation on Claude's own initiative would have been wrong; but re-confirming authorization for the exact named task after the specific flag was explained, with no pushback, is treated as the human's answer to the question that was asked — not silently assumed on a bare repetition of the original blanket command. `PHASE-0.5` was executed under the bare-mechanism interpretation already on record (Observation #1, `.ai/WBS.md` § 4): session/token issuance and secure password storage, explicitly excluding any user/workspace/project model or per-resource authorization. | TASK_BOARD.md (`PHASE-0`, `.5` row and notes), WBS.md § 2 (narrative), § 4 (`PHASE-0.5` status line, Observation #1 resolution), PROJECT_STATE.md §§1–14 (Risk R7 resolved), and the repository itself (1 new subtask commit) |
| Q16 | The human's session-7 command ("execute and Authorize `PHASE-0.5` **then** `PHASE-0.8`") used "then" as a sequencing instruction, not merely a list separator. `PHASE-0.8`'s own stated dependency in `.ai/WBS.md` § 4 is `PHASE-0.5`–`.7` reaching `DONE` — a readiness gate independent of authorization (per `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` § 30, and consistent with how `BOOTSTRAP-002.2`/`.3` stayed `BLOCKED` after authorization until their own prerequisites actually completed, session 2). `PHASE-0.8` was therefore executed only after `PHASE-0.5` (Q15) was confirmed `DONE` — at which point `.8`'s dependency was, for the first time, genuinely satisfied rather than merely cleared by a broader authorization event. `PHASE-0.8` was executed: a four-category test suite (backend-unit, API-integration, contract, frontend-unit) and a ten-job CI pipeline matching `20` § 61's exact stage order. | TASK_BOARD.md (`PHASE-0`, `.8` row and notes; `PHASE-0` summary row now `DONE`/`FULLY AUTHORIZED`), WBS.md § 2 (narrative), § 4 (`PHASE-0.8` status line, corrected stage-list description), PROJECT_STATE.md §§1–14 (all 8 `PHASE-0` subtasks now `DONE`), and the repository itself (1 new subtask commit) |
| Q17 | Human explicitly authorized `PHASE-1` decomposition and explicitly withheld authorization for implementation and for authorizing any individual subtask, in one instruction (session 8, 2026-09-09: "Prepare `PHASE-1` for execution by decomposing it into implementation-ready milestones/tasks... but do not implement or authorize any `PHASE-1` task yet"). Per `03_MASTER_RULES.md` § 2, this is a level-1 explicit human decision, following the identical structure and scope pattern as Q12's `PHASE-0` decomposition authorization: produce the WBS-level breakdown of `PHASE-1` into subtasks with dependencies/required-docs/acceptance-criteria; no code, no scaffolding, no directory/file creation, no subtask granted execution authorization. Applied as: eight subtasks (`PHASE-1.1`–`.8`) decomposed in `.ai/WBS.md` § 4B; two (`PHASE-1.6`, `PHASE-1.7`) additionally flagged `Human Approval Required: Yes` for genuine open scope questions (Observations #7, #8) rather than silently resolved — mirroring the Q10/Q11 and Obs. #1/Q15 precedents of a per-subtask flag surviving a broader decomposition/authorization event. | WBS.md § 2 (task-hierarchy tree), § 4B (new), § 5 (Parallelization notes extended); TASK_BOARD.md (`PHASE-1`, `PHASE-1.1`–`.8` rows and notes); PROJECT_STATE.md §§1–14; README.md (status pointer) |
| Q18 | Human's command named four `PHASE-1` subtasks by ID range, in-conversation (session 9, 2026-09-10: "execute and Authorize `PHASE-1.1` through `PHASE-1.4`"), following the identical Q13/Q14 task-ID-range pattern. None of the four named subtasks carries a `Human Approval Required: Yes` flag (that applies only to `.6`/`.7`, neither named), so — unlike Q14's `PHASE-0.5` case — no separate observation needed surfacing before proceeding; Q18 authorized all four cleanly and immediately. Applied in the order `.1` → **`.4`** → `.2` → `.3` (a self-correction from the originally-stated numeric order, made before any `PHASE-1.2` code was written, once `.ai/WBS.md` §4B's own text for `PHASE-1.4` was found to invoke `03_MASTER_RULES.md` § 97 requiring contracts precede `PHASE-1.2`'s endpoints — disclosed in `TASK_BOARD.md`'s notes, not silently followed). All four subtasks reached `DONE` this session; `PHASE-1.5`–`.8` were not authorized and were not touched. Two new non-blocking observations were disclosed during execution: #10 (`Membership`'s domain-vs-security placement — an implementation-time decision `WBS.md` §4B explicitly deferred to this subtask) and #11 (treating "Organization" as equivalent to `Workspace` for `PHASE-1`, since no capability describes Organization as its own entity). A retroactive, disclosed fix was also applied to already-committed `PHASE-1.1` work: `User`/`Workspace`/`Project` gained a `status`-enum validation check their `__post_init__` methods had been missing since that subtask's original commit, discovered via this session's own deliberate-break testing of the newly-added `Membership` entity. | TASK_BOARD.md (`PHASE-1`, `.1`–`.4` rows and notes, corrected-order disclosure), WBS.md (unchanged structurally — no new subtask added, Membership's shape was already anticipated as an implementation-time decision), PROJECT_STATE.md §§1–14, and the repository itself (baseline-reconstruction commit + one commit per subtask, 5 commits total) |

## 11. Architecture / Documentation / Implementation / Testing / Runtime-Verification Status

| Dimension | Status |
|---|---|
| Architecture | Approved, consolidated (Q1) |
| Documentation | Repaired per Q1/Q2/Q3/Q5/Q6/Q7; Q4 deferred; all 26 docs remain `APPROVED`; unchanged across sessions 2–5 |
| Component Inventory | FOUNDATION + SHARED tiers confirmed, FEATURE tier fully broken down per-workspace (`BOOTSTRAP-002.1`–`.3`). Full component registry populated, per-component ownership assigned with zero open exceptions (Dashboard resolved via Q11), per-component testing requirements defined (`BOOTSTRAP-002.4`–`.6`) |
| Phase-0 Planning | Decomposed into 8 subtasks with dependencies, cited docs, and acceptance criteria (session 5, Q12); all 8 subsequently executed (sessions 6–7) |
| Phase-1 Planning | Decomposed into 8 subtasks with dependencies, cited docs, and acceptance criteria (session 8, Q17); **4 of 8 (`.1`–`.4`) authorized and executed (session 9, Q18).** `.5`–`.8` not authorized. Two subtasks (`.6`, `.7`) still carry an unresolved `Human Approval Required: Yes` scope question (Observations #7, #8, `.ai/WBS.md` § 4B) |
| Implementation | `DONE` for `PHASE-0` — all 8 subtasks executed and `AUTHORIZED` (sessions 6–7, Q13–Q16). **`PHASE-1`: `.1`–`.4` `DONE`/`AUTHORIZED` (session 9, Q18) — real domain entities, persistence, authentication, authorization, and API contracts now exist. `.5`–`.8` `NOT STARTED`.** |
| Testing | `DONE` (foundation-level, `PHASE-0.8`) plus substantial `PHASE-1` expansion (session 9): 89 tests across 13 zero-dependency files genuinely executed (domain entities, use cases with fakes, authorization checks), each category deliberate-break-tested; TypeScript contracts genuinely `tsc`-compiled. The infrastructure/API-dependent remainder awaits a session with package-install access. |
| Runtime Verification | `PARTIAL`, substantially wider than before session 9. Genuinely `RUNTIME_VERIFIED`: `test_error_hierarchy.py` (session 7) plus, new this session, all of `PHASE-1.1`–`.4`'s pure-Python/AST-based/TypeScript-compiled layers (§ 5). `IMPLEMENTED`/`UNVERIFIED` remains the honest status for every layer requiring `fastapi`/`pydantic`/`sqlalchemy`/`alembic`/`asyncpg`/`bcrypt`/`pydantic-settings` to actually execute — see Risk R9 for exactly what a future session needs to do to close this gap. |

## 12. Required Next Action / Authorized Task / Authorization Scope

- **Required next action:** a human decision on where to go from here — `PHASE-0` is entirely `DONE`, and `PHASE-1.1`–`.4` are now `DONE` (session 9, Q18). Options: (1) authorize `PHASE-1.5` (Project Lifecycle) — `READY`, its only dependency (`.1`) satisfied, the natural next step; (2) resolve Observations #7/#8 (`.ai/WBS.md` § 4B — `PHASE-1.6` dashboard scope, `PHASE-1.7` AI-provider choice) before authorizing those two subtasks specifically, unchanged since session 8; (3) a session with package-install, local-Postgres, and npm-registry access to close Risk R9 by runtime-verifying everything `PHASE-0` and `PHASE-1.1`–`.4` already built — doesn't require new authorization; or (4) revisit/re-scope something within `PHASE-1.5`–`.8`'s own decomposition before authorizing further.
- **Open items:** Observation #4 and #5 (naming-collision flags, non-blocking, unchanged). Observation #6 (`PHASE-1.1`'s merge, disclosed, now moot — the subtask is `DONE`). Observation #7 (`PHASE-1.6` dashboard scope) and #8 (`PHASE-1.7` provider choice) — both still open, block only their own subtask's authorization. Observation #9 (`PHASE-1.3`'s resource/tool-level authorization deferred, disclosed, non-blocking — now implemented exactly as disclosed). **New this session:** Observation #10 (`Membership`'s domain-vs-security placement, disclosed, non-blocking, now implemented) and Observation #11 (Organization-as-Workspace-equivalent for `PHASE-1`, disclosed, non-blocking). Risk R9 (runtime-verification gap, now spanning `PHASE-0` and `PHASE-1.1`–`.4`), R12 (account-recovery has no email-delivery mechanism yet), R13 (`packages/contracts`/`backend/app/contracts/api` sync is manual, unenforced) — all non-blocking for further `PHASE-1` authorization.
- **Authorized and completed session 8:** `PHASE-1` decomposition (Q17) — `PHASE-1` broken into `PHASE-1.1`–`.8` in `.ai/WBS.md` § 4B. Explicitly scoped by the human as decomposition-only: no implementation of any subtask, no repository/code changes, no subtask authorization.
- **Authorization scope (session 8):** exactly `PHASE-1`'s decomposition, plus the resulting required updates to `.ai/WBS.md`, `.ai/TASK_BOARD.md`, `.ai/PROJECT_STATE.md`, and `README.md`. No core document (`docs/*.md`) was edited. No `PHASE-1` subtask was authorized, implemented, or scaffolded.
- **Authorized and completed session 9:** `PHASE-1.1` through `PHASE-1.4` (Q18) — core domain model and persistence, API contract layer, authentication, and authorization foundation, all executed and committed. Execution order was `.1` → `.4` → `.2` → `.3` (corrected from the numeric order stated at authorization time — see Q18, § 10). Scope was exactly these four named subtasks; `.5`–`.8` were not authorized and were not touched, even though `.5` and (independently) `.7` became/remained `READY` as a side effect.
- **Authorization scope (session 9):** exactly `PHASE-1.1`–`.4`'s execution, plus the resulting required updates to `.ai/TASK_BOARD.md` and `.ai/PROJECT_STATE.md` (`.ai/WBS.md` required no structural change — `PHASE-1.3`'s `Membership` shape was already anticipated there as an implementation-time decision). No core document (`docs/*.md`) was edited. No `PHASE-1.5`–`.8` work was started or authorized.
- **Authorized and completed session 3:** `BOOTSTRAP-002.4`, `BOOTSTRAP-002.5`, `BOOTSTRAP-002.6`, per the human's explicit in-conversation authorization (Q9, § 10).
- **Authorized and completed session 4:** the Dashboard ownership decision (Q11) — `features/dashboard` assigned as Dashboard's owner. Explicitly scoped by the human as decision-only: no `features/dashboard` scaffolding, no implementation, no `PHASE-0` work.
- **Authorized and completed session 5:** `PHASE-0` decomposition (Q12) — `PHASE-0` broken into `PHASE-0.1`–`.8` in `.ai/WBS.md` § 4. Explicitly scoped by the human as decomposition-only: no implementation of any subtask, no repository, no code.
- **Authorized and completed session 6:** `PHASE-0.1` through `PHASE-0.4` (Q13) — repository bootstrap, environment/configuration, database/migrations, and API foundation, all executed and committed to a newly-initialized git repository. Scope was exactly these four named subtasks; `.5`–`.8` were not authorized and were not touched, even though `.5`–`.7`'s dependencies became satisfied as a side effect of `.1`–`.4` completing.
- **Authorization scope (session 6):** exactly `PHASE-0.1` through `PHASE-0.4`'s execution, plus the resulting required updates to `.ai/WBS.md`, `.ai/TASK_BOARD.md`, and `.ai/PROJECT_STATE.md`. No core document (`docs/*.md`) was edited. No `PHASE-0.5`–`.8` work was started.
- **Authorized and completed session 7:** `PHASE-0.6`/`.7` (Q14) — frontend and observability foundations. `PHASE-0.5` (Q15) — authentication foundation, executed after Observation #1 was surfaced and the human's re-authorization was treated as its resolution. `PHASE-0.8` (Q16) — testing foundation and CI, executed last, once `.5`'s completion genuinely satisfied `.8`'s own stated dependency. **`PHASE-0` is now fully authorized and `DONE` — all 8 subtasks.**
- **Authorization scope (session 7, in total):** `PHASE-0.5` through `.8`'s execution (across two authorization events, Q14/Q15/Q16 — see § 10 for exactly which subtask each command actually authorized), plus the resulting required updates to `.ai/WBS.md`, `.ai/TASK_BOARD.md`, and `.ai/PROJECT_STATE.md`. No core document (`docs/*.md`) was edited. No `PHASE-1` work was started or authorized.
- **Authorized and completed session 8:** `PHASE-1` decomposition (Q17) — `PHASE-1` broken into `PHASE-1.1`–`.8` in `.ai/WBS.md` § 4B. Explicitly scoped by the human as decomposition-only: no implementation of any subtask, no repository/code changes, no subtask authorization.
- **Authorization scope (session 8):** exactly `PHASE-1`'s decomposition, plus the resulting required updates to `.ai/WBS.md`, `.ai/TASK_BOARD.md`, `.ai/PROJECT_STATE.md`, and `README.md`. No core document (`docs/*.md`) was edited. No `PHASE-1` subtask was authorized, implemented, or scaffolded.

## 13. Current Dependencies / Context Requirements

See `.ai/WBS.md` § 1 for the persisted document dependency graph, and `.ai/SESSION_PROMPT.md` for the task-specific context-retrieval procedure (built on `25_CONTEXT_MANAGEMENT.md`, not duplicating it).

## 14. Verification Log

**Session 1 (`BOOTSTRAP-001`), 2026-09-06:**
- ✅ Grep-verified: no unintended residual "Page Map" occurrences remain.
- ✅ Grep-verified: `03` § 2 names the PRD.
- ✅ Grep-verified: `23` footer `verification_states` is the clean 7-value list.
- ✅ Grep-verified: retired hierarchy phrases ("AI Assumptions" as a bare final tier, "Unverified Assumption", "Validated Structured Data") no longer appear in `23` or `25`.

**Session 2 (`BOOTSTRAP-002.1`–`.3`), 2026-09-06:**
- ✅ Script-verified: zero component-name overlap between FOUNDATION and SHARED tier tables in `.ai/COMPONENT_MATRIX.md`.
- ✅ Manually verified: no FEATURE-tier component name literally duplicates a SHARED-tier shell name; every instance-of-a-shared-shell relationship is called out in the Notes column.
- ✅ Manually verified: every row across all three tiers cites at least one named source-document section.
- ❌ Not verified: rendered Markdown appearance in a viewer/IDE; no linter exists yet to run against the docs.
- ❌ Not verified / not applicable: anything about running code, since none exists. `BOOTSTRAP-002.1`–`.3` are documentation-derivation tasks; there is nothing to unit-test.

**Session 3 (`BOOTSTRAP-002.4`–`.6`), 2026-09-06:**
- ✅ Read `19_DESIGN_SYSTEM.md` §§13–20, 21–30, 92–97, 108–116, 127–136; `18_FRONTEND_ARCHITECTURE.md` §§8–12; `22_TESTING_AND_VALIDATION.md` §§48–49, 76–78 (section map obtained first via `grep` of the doc's own numbered headers, then targeted reads — not a blind full read of all three documents).
- ✅ Manually verified: every cell in `COMPONENT_MATRIX.md` §§5.1–5.3 traces to either a §5.0 tier-default citation or an inline component-specific citation. No blank/unsourced field.
- ✅ Manually verified: `OWNERSHIP.md`'s per-component table assigns exactly one owner to every component except the Dashboard group, which is explicitly (not silently) left open.
- ✅ Verified, before starting any derivation: the bundled `Seonex-main-bootstrapped-v2.zip` archive's `.ai/COMPONENT_MATRIX.md` is byte-identical to the un-populated version despite its accompanying `TASK_BOARD.md` claiming `BOOTSTRAP-002.4` (Populate Component Registry) was complete, and its `TASK_BOARD.md` cites an `OWNERSHIP.md` § "Per-Component Ownership" that does not exist in that same archive's actual `OWNERSHIP.md`. Confirmed by direct `diff` and `grep`, not assumed.
- ❌ Not verified: rendered Markdown appearance in a viewer/IDE.
- ❌ Not verified / not applicable: anything about running code, since none exists. `BOOTSTRAP-002.4`–`.6` are documentation-derivation tasks; there is nothing to unit-test or run.

**Session 4 (Dashboard ownership decision, Q11), 2026-09-06:**
- ✅ Ran `grep -n "Dashboard" *.md` across all five `.ai/` files before editing, to get a complete list of every place the prior contested status was represented, so none would be missed.
- ✅ Re-ran the same `grep` after editing; manually reviewed every hit to confirm each now states the resolved owner (`features/dashboard`) or is an explicitly-labeled historical/traceability note, with no remaining "contested," "unassigned," or "route-level, no owner" language.
- ✅ Manually verified `COMPONENT_MATRIX.md` §§4, 5.3, 5.4, 6 and `OWNERSHIP.md` all agree on Dashboard's owner — no cross-file contradiction.
- ✅ Confirmed the `BOOTSTRAP-002.5` acceptance criterion ("every component has exactly one owner," `WBS.md` §3) now holds with zero exceptions.
- ✅ Confirmed no edits were made to any of the 26 core documents (checked: none were opened for editing this session, only `.ai/` files) and no files or directories were created outside `.ai/` — in particular, no `features/dashboard` directory exists anywhere in the project.
- ❌ Not verified / not applicable: anything about running code, since none exists and none was created.

**Session 5 (`PHASE-0` decomposition, Q12), 2026-09-06:**
- ✅ Obtained section maps for `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md`, `20_PROJECT_STRUCTURE.md`, `07_TECHNICAL_ARCHITECTURE.md`, and `22_TESTING_AND_VALIDATION.md` via `grep` of numbered headers first, then targeted reads of the sections cited in `.ai/WBS.md` § 4 — not a blind full read of any of the four documents.
- ✅ Manually verified every `PHASE-0.x` subtask (`.1`–`.8`) carries at least one named source-document section citation for both scope and acceptance criteria.
- ✅ Manually re-derived the dependency graph among `PHASE-0.1`–`.8` and confirmed no cycles and no forward references.
- ✅ Verified, before writing anything: `BOOTSTRAP-002`'s Lifecycle Status is `DONE` in the pre-existing `.ai/TASK_BOARD.md` (`PHASE-0.1`'s sole dependency), by direct read — not assumed from conversation memory.
- ✅ Ran a directory snapshot before and after this session's edits; confirmed the only files touched are `.ai/WBS.md`, `.ai/TASK_BOARD.md`, and this file — no `apps/`, `backend/`, `packages/`, or `infrastructure/` directory was created, and none of the 26 `docs/*.md` files were modified.
- ✅ Confirmed every `PHASE-0` and `PHASE-0.x` row in `.ai/TASK_BOARD.md` reads Authorization `NOT AUTHORIZED` and Verification Status `UNVERIFIED` after editing — no row overstates its status.
- ❌ Not verified: rendered Markdown appearance in a viewer/IDE.
- ❌ Not verified / not applicable: anything about running code, tests, or a live repository, since none exists and none was created or authorized this session.

**Session 6 (`PHASE-0.1`–`.4` execution, Q13), 2026-09-07:**
- ✅ Verified, before writing anything: diffed the standalone `PROJECT_STATE.md`/`WBS.md`/`TASK_BOARD.md` uploads against the copies bundled inside `Seonex-main.zip`'s `.ai/` directory — byte-identical, no repeat of the session-3 fabricated-archive discrepancy (R6). Confirmed by direct `diff`, not assumed.
- ✅ Ran a tooling diagnostic *before* writing code: `python3`/`git`/`node`/`npm` present; `fastapi`/`uvicorn`/`pydantic`/`pydantic_settings`/`sqlalchemy`/`alembic`/`asyncpg`/`psycopg2`/`pytest`/`httpx` all absent; `pip install fastapi --dry-run` returned "No matching distribution found" (no network); `which psql pg_ctl postgres` returned nothing. This is why `PHASE-0.3`/`.4`'s live-execution criteria are recorded `UNVERIFIED`/`PARTIALLY_VERIFIED` rather than skipped or assumed passing.
- ✅ Read only the specific cited sections of `07_TECHNICAL_ARCHITECTURE.md` and `20_PROJECT_STRUCTURE.md` that `.ai/WBS.md` § 4 names for `PHASE-0.1`–`.4` (§§3-4,58-59,26-30,15-20 of the relevant docs, plus layer-boundary sections 5-14 of `07` for writing accurate module docstrings) — not a blind full read of either document, per `.ai/SESSION_PROMPT.md`'s Context Retrieval Procedure.
- ✅ `git init`; first commit imports the pre-existing baseline (26 docs + `.ai/`) before any `PHASE-0` code, so the repository's own history shows what existed before this session versus what this session added.
- ✅ One commit per subtask (`.1`,`.2`,`.3`,`.4`), each with a message citing its acceptance criteria and what was/wasn't verified — `git log --oneline` confirms 5 commits total (baseline + 4 subtasks).
- ✅ Automated `ast.parse` syntax check across all 25 new/changed `.py` files (enumerated via `git diff --name-only` + `git ls-files --others`, so none could be missed) — zero syntax errors.
- ✅ `git check-ignore -v` against 5 representative paths confirmed `.gitignore` correctly ignores Python cache/`.venv`/`.env` artifacts while correctly tracking `.env.example` and migration files.
- ✅ `grep`-verified zero raw-SQL string concatenation across all `PHASE-0.3` database code (criterion 3 of `PHASE-0.3`, fully satisfied).
- ✅ `grep`/manually verified all 10 `AppError` subclasses' `http_status` values against `07` § 20's table exactly; verified all 3 exception handlers (`AppError`/`RequestValidationError`/`Exception`) are registered; verified `request_id` flows from middleware into the error envelope (criteria 3-4 of `PHASE-0.4`, fully satisfied).
- ❌ NOT verified / explicitly flagged: `PHASE-0.3` criteria 1-2 (DB reachable; migration applies/reverts) and `PHASE-0.4` criteria 1-2 (API boots; health responds) — no package-install or database access in this sandbox. Recorded as Risk R9, not silently passed over.
- ❌ Not verified: rendered Markdown appearance in a viewer/IDE.
- ❌ Not applicable: CI, since `PHASE-0.8` (which would establish it) remains unauthorized.

**Session 7 (`PHASE-0.6`–`.7` execution, Q14), 2026-09-08:**
- ✅ Verified, before writing anything: the uploaded `seonext-main.zip`'s `.ai/PROJECT_STATE.md`/`TASK_BOARD.md`/WBS.md` were read directly rather than relied on from prior-session conversation memory, per `SESSION_PROMPT.md`'s explicit instruction that `.ai/*.md` wins over conversation history. Also diffed the three standalone `.md` uploads against their `.ai/`-directory copies inside the zip — byte-identical, no repeat of the session-3 fabricated-archive discrepancy (R6).
- ✅ Confirmed, before authorizing execution of anything: `PHASE-0.5`'s `Human Approval Required: Yes` flag (Observation #1) in `.ai/WBS.md` § 4 — read directly, not assumed from the human's phrasing — and that `PHASE-0.8`'s Lifecycle Status was `BACKLOG` with an explicit blocked-on-`.5`–`.7` note, before deciding what the session-7 command could and couldn't execute.
- ✅ Ran a tooling diagnostic before writing code: `python3`/`git`/`node`/`npm`/`npx`/`tsc` present this session (unlike session 6, which had no `node`); `fastapi`/`pydantic`/`sqlalchemy` still not importable; `npm ping` and `curl -I https://registry.npmjs.org` both returned `host_not_allowed` via the egress proxy; `pip install --break-system-packages --dry-run` confirmed no PyPI access either. This is why `PHASE-0.6`'s "frontend runs locally" and `PHASE-0.7`'s `prometheus_client` import remain `UNVERIFIED`.
- ✅ Read only the specific cited sections of `20_PROJECT_STRUCTURE.md` (§§3-4, 24, 25-27, 32, 33, 61-62) and `07_TECHNICAL_ARCHITECTURE.md` (§3) that `.ai/WBS.md` § 4 names for `PHASE-0.6`/`.7`, plus the already-written `PHASE-0.1`–`.4` source files (`main.py`, `settings.py`, error handlers, etc.) to match established conventions — not a blind full read of either document.
- ✅ Discovered `.git` was not part of the uploaded archive; re-extracted a fresh copy of `seonext-main.zip`, initialized git there, and confirmed via `diff -rq` (excluding `.git`) that the baseline commit exactly matches the upload before adding any session-7 change.
- ✅ One commit per subtask (`.6`, `.7`) after the baseline commit — `git log --oneline` confirms 3 commits total.
- ✅ Automated `ast.parse` syntax check across all 50 backend `.py` files (25 pre-existing + 25 new/changed) in a single pass — zero syntax errors, re-run against the final assembled repo tree immediately before the `.7` commit (not just the scratch working copy).
- ✅ AST-level (not `grep`-level) check of every `logger.*()` call site in `backend/app/`, confirming each carries a structured `extra=` keyword argument — zero raw-string log calls exist.
- ✅ `grep`-reconfirmed zero raw-SQL string concatenation across the full `backend/` tree, to catch any regression `.7`'s changes might have introduced into `.3`'s territory (none found).
- ✅ Validated all 5 new JSON config files via `python3 json.load`.
- ✅ Syntax-spot-checked `apps/web`'s 3 `.ts`/`.tsx` files with the sandbox's global `tsc` (v6.0.3) — no syntax errors; module-resolution errors for `react`/`next` were expected (no `node_modules`) and not treated as defects.
- ❌ NOT verified / explicitly flagged: `PHASE-0.6` criterion 1 (frontend runs locally) and `PHASE-0.7` criterion 2's actual `prometheus_client` behavior — no `npm`-registry or PyPI access in this sandbox. Recorded as an extension of Risk R9, not silently passed over.
- ❌ Not verified: rendered Markdown appearance in a viewer/IDE; actual browser rendering of `apps/web`'s placeholder page.
- ❌ Not applicable: CI, since `PHASE-0.8` remains unauthorized; `PHASE-0.5`, since it was not executed this session.

**Session 7, continued (`PHASE-0.5` execution, Q15), 2026-09-08:**
- ✅ Verified before writing any code: re-read `.ai/WBS.md` §4's Observation #1 text directly, confirmed the human's re-authorization arrived immediately after that flag's substance had been explained in the prior turn, and recorded the reasoning for treating it as resolved (Q15) *before* starting implementation, not after.
- ✅ Read the specific cited sections of `07_TECHNICAL_ARCHITECTURE.md` (§§53–56) and `20_PROJECT_STRUCTURE.md` (§23) that `.ai/WBS.md` §4 names for `PHASE-0.5` — not a blind full read.
- ✅ Ran a throwaway script exercising PyJWT's real installed API (2.7.0) *before* writing `tokens.py` against it: confirmed `jwt.encode`/`jwt.decode`'s exact signatures and return types, confirmed an expired token raises `ExpiredSignatureError`, confirmed a wrong-secret decode raises `InvalidTokenError`/`InvalidSignatureError` — all matching what the code was then written to assume.
- ✅ Grep-confirmed zero role/permission/authorization logic anywhere under `backend/app/security/` (acceptance criterion 4) — a targeted case-insensitive search for `role|permission|is_allowed|has_access|can_access`, zero matches outside explanatory docstrings.
- ✅ Grep-confirmed `tokens.py` does not import from `backend/app/api/` (dependency-direction rule, `20` §2.2).
- ✅ Automated `ast.parse` syntax check across all 59 backend `.py` files at that point in the session — zero errors.
- ❌ NOT verified: the module's actual runtime behavior end-to-end — `pydantic-settings` (via `get_settings()`) and `bcrypt` are both absent from this sandbox. Recorded as an extension of Risk R9.

**Session 7, continued (`PHASE-0.8` execution, Q16), 2026-09-08:**
- ✅ Verified before starting: `PHASE-0.5`'s Lifecycle Status actually read `DONE` in `.ai/TASK_BOARD.md` (not merely planned) before beginning `PHASE-0.8` — confirming `.8`'s own stated dependency was genuinely satisfied, per this project's standing discipline of checking actual completion, not planned completion.
- ✅ Read the specific cited sections of `22_TESTING_AND_VALIDATION.md` (§120, plus §§105, 119 for test-naming/architecture conventions) and `20_PROJECT_STRUCTURE.md` (§§33, 61) that `.ai/WBS.md` §4 names for `PHASE-0.8`.
- ✅ Before removing `backend/app/api/errors/__init__.py`'s `fastapi`-dependent re-export, grepped the entire codebase for every import of `register_exception_handlers` and confirmed all of them (just `backend/app/main.py`) already went through `.handlers` directly — confirmed non-breaking *before* making the change, not assumed safe afterward.
- ✅ **Actually executed** `backend/tests/unit/test_error_hierarchy.py` (`PYTHONPATH=. python3 backend/tests/unit/test_error_hierarchy.py`): all 6 tests passed.
- ✅ **Actually proved acceptance criterion 3**, not merely asserted it: backed up `exceptions.py`, changed `AuthenticationError.http_status` from `401` to `200`, re-ran the same test command, confirmed it failed with `AssertionError: AuthenticationError.http_status was 200, expected 401 per docs/07_TECHNICAL_ARCHITECTURE.md §20` — the correct, specific diagnostic — then restored the file from backup, `diff`-confirmed byte-identical to the pre-break version, and re-ran the suite a third time to confirm it passed again.
- ✅ Automated `ast.parse` syntax check across all 64 backend `.py` files (the final count after `PHASE-0.8`'s additions) in a single pass, re-run immediately before the `PHASE-0.8` commit.
- ✅ Validated `pyproject.toml` (`tomllib.load`), `.github/workflows/ci.yml` (`yaml.safe_load` — also printed the resolved job list and `needs:` chain to confirm the sequential dependency order matches `20` §61 exactly), and all new/changed JSON files (`json.load`).
- ✅ Syntax-spot-checked the three new/changed TypeScript files (`health.test.ts`, `vitest.config.ts`, `eslint.config.mjs`) with the sandbox's global `tsc` — every error surfaced was a module-resolution error traceable to the absent `node_modules` (`vitest`, `@eslint/eslintrc`, `@types/node`), none a genuine syntax defect.
- ✅ Discovered, mid-commit-message-drafting, that backtick characters inside a `bash_tool`-executed `git commit -m "..."` double-quoted string get interpreted as shell command substitution — the first `PHASE-0.8` commit message was silently corrupted (` `` python -m build `` ` and a similar snippet were replaced by their command's failed output). Caught by reading back `git log -1 --format=%B` immediately after committing rather than assuming the commit succeeded as drafted; fixed via `git commit --amend` with backtick-free wording, then re-verified the corrected message contained no further artifacts.
- ❌ NOT verified: `test_authentication_tokens.py`, `test_health_endpoint.py`, `test_response_schemas.py`, `health.test.ts` — each needs a package this sandbox cannot install (`pydantic-settings`/`bcrypt`, `fastapi`/`httpx`, `pydantic`, `vitest`/`npm install`, respectively). The CI workflow itself has never run on a real GitHub Actions runner. All recorded as extensions of Risk R9, not silently passed over.
- ❌ Not verified: rendered Markdown appearance in a viewer/IDE.

**Session 8 (`PHASE-1` decomposition, Q17), 2026-09-09:**
- ✅ Verified, before writing anything: read `.ai/PROJECT_STATE.md`, `.ai/TASK_BOARD.md`, and `.ai/WBS.md` directly from the uploaded `seonext-main.zip` rather than relying on prior-session conversation memory, per `SESSION_PROMPT.md`'s explicit instruction that `.ai/*.md` wins over conversation history. Confirmed directly (not assumed) that all 8 `PHASE-0` subtasks read Lifecycle Status `DONE` and Authorization `AUTHORIZED` before treating any of them as a satisfied `PHASE-1` dependency.
- ✅ Obtained section maps via `grep` of numbered headers first for `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md`, `06_DATA_ARCHITECTURE.md`, `07_TECHNICAL_ARCHITECTURE.md`, `16_OUTPUT_CONTRACTS.md`, `17_UI_UX_SPECIFICATION.md`, `20_PROJECT_STRUCTURE.md`, `22_TESTING_AND_VALIDATION.md`, and `03_MASTER_RULES.md`, then read only the specific sections cited in `.ai/WBS.md` § 4B — not a blind full read of any of the eight documents.
- ✅ Directly inspected the repository (not just the docs) before decomposing: confirmed `backend/app/domain/`, `backend/app/contracts/`, `backend/app/context/`, `backend/app/security/{authorization,permissions,tenancy}/`, `integrations/`, and `packages/contracts/` all still hold only placeholder `__init__.py`/`README.md` files, several already reading "Reserved for `PHASE-1`" in their own docstrings — grounding the decomposition in the actual current codebase, not assumptions from the docs alone.
- ✅ Manually verified every `PHASE-1.x` subtask (`.1`–`.8`) carries at least one named source-document section citation for both scope and acceptance criteria — no unsourced subtask.
- ✅ Manually re-derived the `PHASE-1` dependency graph and confirmed it forms a valid DAG with no cycles, no forward reference to a not-yet-defined subtask, and no accidental dependency on `PHASE-2`+ territory.
- ✅ Performed the `24` §§97–98 Gate/Anti-Pattern self-check against the decomposition as a whole (recorded in `.ai/WBS.md` § 4B's "Decomposition Method" subsection) before finalizing any individual subtask.
- ✅ Identified and disclosed four non-blocking observations (#6–#9) rather than silently resolving any of them; two (#7, #8) were additionally flagged `Human Approval Required: Yes` on their respective subtasks (`PHASE-1.6`, `PHASE-1.7`), per `SESSION_PROMPT.md`'s Non-Negotiables.
- ✅ Cross-checked `.ai/WBS.md`, `.ai/TASK_BOARD.md`, and this file against each other after editing: all three agree `PHASE-1.1`/`.7` are `READY`, the other six are `BACKLOG`, and no `PHASE-1` subtask's Authorization column reads anything other than `NOT AUTHORIZED`.
- ✅ Ran a full repository file-listing snapshot (`find . -type f`, excluding `.git`) before this session's first edit and again after its last edit — `diff` of the two listings is empty (zero files added, zero removed). `git status --short` after all edits shows exactly four modified files (`.ai/WBS.md`, `.ai/TASK_BOARD.md`, `.ai/PROJECT_STATE.md`, `README.md`) and nothing else — confirmed directly, not assumed, and consistent with the human's instruction to decompose and update control files only.
- ✅ Confirmed no `docs/*.md` file appears in that same `git status --short` output — no core document was touched.
- ❌ Not verified / not applicable: anything about running code, tests, or a live repository — no `PHASE-1` code exists and none was authorized or created this session.
- ❌ Not verified: rendered Markdown appearance in a viewer/IDE.

**Session 9 (`PHASE-1.1`, `PHASE-1.4`, `PHASE-1.2`, `PHASE-1.3` execution, Q18), 2026-09-10:**
- ✅ Verified, before writing anything: extracted the uploaded `seonext-main.zip` fresh and read `.ai/PROJECT_STATE.md`, `.ai/TASK_BOARD.md`, `.ai/WBS.md` directly from it rather than relying on prior-session conversation memory — confirmed directly that session 8's `PHASE-1` decomposition was genuinely complete in the uploaded files (all eight subtasks fully specified in `WBS.md` §4B), and that `PHASE-1.1`/`.7` read `READY` while `.2`–`.6`/`.8` read `BACKLOG`.
- ✅ Confirmed directly, not assumed from session 7's notes: fresh `import` attempts for `fastapi`/`pydantic`/`pydantic_settings`/`sqlalchemy`/`asyncpg`/`alembic`/`bcrypt`/`prometheus_client`/`pytest` all raised `ModuleNotFoundError`; `jwt` (PyJWT 2.7.0) imported successfully. `pip install --dry-run sqlalchemy` returned "No matching distribution found"; `curl -I https://pypi.org` returned HTTP 403 with `x-deny-reason: host_not_allowed`. Node (`v22.22.2`), `npm`, and a global `tsc` (`6.0.3`) were found present and a trivial `.ts` file was confirmed to compile — a capability check done directly, not assumed from any prior session.
- ✅ Read the specific cited sections of `06_DATA_ARCHITECTURE.md` (§§5–8, 54–55), `07_TECHNICAL_ARCHITECTURE.md` (§§14–20, 26–30, 53–56), `03_MASTER_RULES.md` (§§97, 108–109, 144–145), `20_PROJECT_STRUCTURE.md` (§§10, 16, 29–30, 32, 54), `16_OUTPUT_CONTRACTS.md` (§§4–12), and `22_TESTING_AND_VALIDATION.md` (§§14, 52, 105, 108, 119) that `.ai/WBS.md` §4B names for `PHASE-1.1`–`.4` — section maps obtained via `grep` of each document's own numbered headers first, then targeted reads, not a blind full read of any document.
- ✅ Inspected the existing `PHASE-0` codebase directly before writing anything new: `backend/app/infrastructure/{config,database}/`, `backend/app/api/{errors,routes,schemas,dependencies}/`, `backend/app/security/authentication/{passwords,tokens}.py` were all read in full to confirm exact function signatures, conventions, and integration points before building on top of them.
- ✅ Reconstructed git history fresh (the uploaded archive again had no `.git`, same as sessions 6–7's finding — confirmed directly this session, not assumed) and verified the baseline commit's tree is byte-identical to the uploaded archive via `diff -rq`, before making any change.
- ✅ Found and corrected, before any `PHASE-1.2` code was written: `.ai/WBS.md` §4B's own `PHASE-1.4` text invokes `03` §97 to require contracts precede `PHASE-1.2`'s endpoints — re-read carefully enough to catch that the originally-stated execution order (`.1`→`.2`→`.3`→`.4`) would have violated this, and corrected the order (disclosed in `TASK_BOARD.md`, not silently followed) before starting `.2`.
- ✅ **`PHASE-1.1`:** all 5 acceptance criteria checked directly against the written code (domain entities with no infra imports — confirmed via `ast.parse`-based inspection of import statements; stable IDs via `new_id()`; migration structural symmetry via a dedicated AST test; `ProjectRepository.get()`'s required-both-parameters shape via a dedicated AST test; FK/uniqueness constraints present in both the ORM model and the migration, cross-checked line-by-line for consistency).
- ✅ **`PHASE-1.1` tests:** `python3 -m backend.tests.unit.test_domain_{user,workspace,project}` each actually run (13/7/10 tests passing initially); `test_project_repository_tenant_scoping` (6 tests) and `test_migration_structural_reversibility` (4 tests) each actually run. Deliberate breaks introduced and confirmed to fail, then reverted, for: `User`'s empty-hashed-password check, `Workspace`'s empty-name check, `Project`'s invalid-workspace_id check, `ProjectRepository.get()`'s required-parameter shape (made `workspace_id` optional with a default), and the migration's `downgrade()` ordering (swapped/duplicated a drop call).
- ✅ **`PHASE-1.4`:** all 4 acceptance criteria checked directly (contracts for every `PHASE-1.2` endpoint present before `.2` was implemented; `ErrorResponse` reused unchanged, confirmed via `diff`-equivalent read against the `PHASE-0.4` original; `packages/contracts`/`backend/app/contracts/api` manually cross-checked field-for-field; `SuccessEnvelope`'s `status` literal plus reused `ErrorResponse.error.code` values distinguish the four response categories §18 names).
- ✅ **`PHASE-1.4` tests:** `test_auth_contracts_no_credential_leak.py` (3 tests) actually run; a deliberately reintroduced `hashed_password` field on `RegisterData` was confirmed to fail the check, then reverted. `tsc -p packages/contracts/tsconfig.json` actually run — exit 0. A scratch consuming file with two deliberate type errors was compiled, confirmed to produce `TS2322`/`TS2741` diagnostics, then deleted; the package was re-compiled clean immediately after.
- ✅ **`PHASE-1.2`:** all 4 acceptance criteria checked directly (register calls the real, injected `hash_password` — confirmed by reading the route's composition-root wiring; login's `issue_access_token(user.id)` call confirmed to use the real domain `User.id`, not a caller-supplied value; no response schema includes `hashed_password`, mechanically confirmed by `PHASE-1.4`'s own test; account recovery's token-issuance/consumption round trip confirmed by a dedicated test, with the delivery gap disclosed as Risk R12 rather than hidden).
- ✅ **`PHASE-1.2` tests:** all five use cases genuinely imported (confirmed zero `ModuleNotFoundError` despite `bcrypt`/`sqlalchemy` being absent — proving the `Protocol`-injection design actually achieves its stated goal, not just in theory) and exercised via `asyncio.run` against `backend/tests/unit/_auth_fakes.py`. 3+5+1+7 = 16 tests run. Three deliberate breaks introduced and confirmed to fail, then reverted: `register_user`'s duplicate-email check disabled; `login_user`'s password-verification check disabled; `confirm_password_reset`'s disabled-account check dropped.
- ✅ **`PHASE-1.3`:** all 4 acceptance criteria checked directly against dedicated tests (criterion 1: negative test + lateral-movement variant; criterion 2: `grep`-equivalent read confirming no function in `security/authorization`/`security/tenancy` accepts a role/membership as a parameter — every check re-derives it from the injected `MembershipReader`; criterion 3: `permissions/roles.py` confirmed to define only the 3-tier hierarchy, nothing action-specific; criterion 4: privilege-escalation and cross-project-leakage tests both present and named to match the acceptance criterion's own language).
- ✅ **`PHASE-1.3` tests:** `test_domain_membership.py` (8 tests, including 2 added mid-session once a coverage gap was found — see below) and `test_authorization_checks.py` (12 tests) actually run against `backend/tests/unit/_authorization_fakes.py`. **Four separate deliberate-break rounds**, each confirmed to fail the corresponding test then reverted: disabling `require_workspace_access` entirely; making `role_meets_minimum` always return `True`; making the fake `ProjectRepository` ignore `workspace_id`; reordering `require_project_access`'s two checks. **The fourth break initially produced no test failure** — traced to `test_non_member_is_rejected_before_any_project_lookup_even_for_a_real_project_id` using a project that legitimately existed in the workspace being probed, so both check-orderings produced the same final exception. The test was rewritten to use a project existing in a *different* workspace, re-run clean, then re-broken and this time correctly caught with the diagnostic `"got ProjectNotFoundError instead of AuthorizationDeniedError — the project lookup ran before the membership check"`.
- ✅ **Retroactive fix, `PHASE-1.1`:** the first deliberate-break attempt on `Membership`'s role validation (removing the `isinstance(self.role, Role)` check) was **not caught by any existing test** — discovered mid-verification, not assumed clean. Added `test_invalid_role_is_rejected`/`test_change_role_to_something_invalid_is_rejected`, re-ran the break, confirmed caught this time. While fixing this, checked `User`/`Workspace`/`Project` (`PHASE-1.1`, already committed) for the identical gap — found it (no `isinstance(self.status, ...)` check existed in any of the three) — fixed all three with a one-line check plus one new test each, re-ran the full domain suite (41 tests) to confirm the fix and zero regressions.
- ✅ Full regression run after every subtask: all previously-passing test files re-run together, confirming zero regressions at each of the four commit points. Final count: 89 tests across 13 files, all genuinely executed, all passing. `python3 -m compileall -q backend infrastructure` run clean at each commit point (zero syntax errors across the entire touched tree).
- ✅ Confirmed via `git status --short`/`git log --oneline` after each commit that exactly the intended files were staged, and that commit messages read back correctly via `git log -1 --format=%B` (no backtick-corruption repeat of session 7's finding — none of this session's commit messages used backticks inside the `git commit -m` string in a way that could trigger shell substitution... actually they did use backticks for Markdown code-formatting, so each commit message was read back and visually confirmed to match what was intended, not merely assumed safe).
- ✅ Checked `.ai/COMPONENT_MATRIX.md` and `.ai/OWNERSHIP.md` per the Session Start Protocol's instruction to do so when a task touches a cross-subsystem boundary — read both in full, confirmed neither needs updating: `COMPONENT_MATRIX.md` is exclusively frontend/design-system taxonomy (untouched by this session's 100%-backend work), and `OWNERSHIP.md`'s "Backend"/"Data"/"Integrations" rows already cover everything this session touched at the document-ownership level with no new contradiction introduced.
- ❌ Not verified: rendered Markdown appearance in a viewer/IDE.
- ❌ Not verified (Risk R9, R12, R13 — see § 9 for exactly what remains and why): SQLAlchemy models/repositories/migrations against a real database; the FastAPI routes/dependencies as real HTTP endpoints; `bcrypt`/`pydantic-settings`'s actual behavior when the real (non-fake) implementations are exercised; semantic (as opposed to declared-shape) agreement between the Python and TypeScript contracts; real email delivery for password reset.
