# TASK_BOARD.md — Operational Task Board

Answers "what are we doing right now and what's its current execution state." For "what must be built and what depends on what," see `.ai/WBS.md`. Two independent status fields per task, per the Q2 resolution — never conflate them:

- **Lifecycle Status** (`23_PROJECT_CONTROL_CENTER.md` § 33): `BACKLOG · READY · AUTHORIZED · IN_PROGRESS · BLOCKED · IN_REVIEW · TESTING · VALIDATING · DONE · REJECTED · CANCELLED`
- **Verification Status** (`26_SKILLS_AND_TOOLING_POLICY.md` § 106): `IMPLEMENTED · TESTED · VALIDATED · RUNTIME_VERIFIED · PARTIALLY_VERIFIED · UNVERIFIED · BLOCKED`

---

## Active Board

| Task ID | Task | Parent | Priority | Lifecycle Status | Verification Status | Blockers | Owner | Authorization | Last Updated |
|---|---|---|---|---|---|---|---|---|---|
| `BOOTSTRAP-001` | Documentation Audit & Control System Init | — | Critical | `DONE` | `IMPLEMENTED` (docs/control-system); `NOT_APPLICABLE` (runtime) | none | AI Agent (this session) | **AUTHORIZED** (explicit, this session) | 2026-09-06 |
| `BOOTSTRAP-001.1a` | Produce bootstrap audit | BOOTSTRAP-001 | Critical | `DONE` | `VALIDATED` (human reviewed and responded to Q1–Q7) | none | AI Agent | AUTHORIZED | 2026-09-06 |
| `BOOTSTRAP-001.1b` | Apply approved documentation repairs | BOOTSTRAP-001 | Critical | `DONE` | `IMPLEMENTED`, grep-`VALIDATED`; not human-re-read line-by-line (`PARTIALLY_VERIFIED`) | none | AI Agent | AUTHORIZED | 2026-09-06 |
| `BOOTSTRAP-001.1c` | Build & persist dependency graph | BOOTSTRAP-001 | High | `DONE` | `IMPLEMENTED` | none | AI Agent | AUTHORIZED | 2026-09-06 |
| `BOOTSTRAP-001.1d` | Create PROJECT_STATE / WBS / TASK_BOARD | BOOTSTRAP-001 | Critical | `DONE` | `IMPLEMENTED` | none | AI Agent | AUTHORIZED | 2026-09-06 |
| `BOOTSTRAP-001.1e` | Create COMPONENT_MATRIX / OWNERSHIP | BOOTSTRAP-001 | High | `DONE` | `IMPLEMENTED` | none | AI Agent | AUTHORIZED | 2026-09-06 |
| `BOOTSTRAP-001.1f` | Create SESSION_PROMPT | BOOTSTRAP-001 | High | `DONE` | `IMPLEMENTED` | none | AI Agent | AUTHORIZED | 2026-09-06 |
| `BOOTSTRAP-001.1g` | Establish state; identify first READY task | BOOTSTRAP-001 | Critical | `DONE` | `IMPLEMENTED` | none | AI Agent | AUTHORIZED | 2026-09-06 |
| `BOOTSTRAP-002` | Foundation Component System | — | High | `DONE` (2.1–2.6 all complete; Dashboard ownership resolved by explicit human decision — see notes) | `IMPLEMENTED` | none | AI Agent (this session) | **AUTHORIZED** (explicit human authorization: 2.1–2.3 session 2; 2.4–2.6 session 3 — Q9; Dashboard ownership decision — Q11) | 2026-09-06 |
| `BOOTSTRAP-002.1` | Derive Foundation component inventory | BOOTSTRAP-002 | High | `DONE` | `IMPLEMENTED` (documentation-consistency verified — see § Verification below) | none | AI Agent (this session) | **AUTHORIZED** (explicit human authorization, this session) | 2026-09-06 |
| `BOOTSTRAP-002.2` | Derive Shared component inventory | BOOTSTRAP-002 | High | `DONE` | `IMPLEMENTED` (documentation-consistency verified) | none | AI Agent (this session) | **AUTHORIZED** (explicit human authorization, this session) | 2026-09-06 |
| `BOOTSTRAP-002.3` | Derive Feature component inventory | BOOTSTRAP-002 | Medium | `DONE` | `IMPLEMENTED` (documentation-consistency verified) | none | AI Agent (this session) | **AUTHORIZED** (explicit human authorization, this session) | 2026-09-06 |
| `BOOTSTRAP-002.4` | Populate Component Registry | BOOTSTRAP-002 | Medium | `DONE` | `IMPLEMENTED` (documentation-derivation verified — every schema field traces to a cited source section; see `COMPONENT_MATRIX.md` § 5) | none | AI Agent (this session) | **AUTHORIZED** (explicit human authorization, this session — see Q9) | 2026-09-06 |
| `BOOTSTRAP-002.5` | Finalize Ownership mapping | BOOTSTRAP-002 | Medium | `DONE` | `IMPLEMENTED` — every component now has exactly one owner; the Dashboard exception was resolved by explicit human decision (Q11) assigning it to a new `features/dashboard` module — see `OWNERSHIP.md` § "Per-Component Ownership" and `COMPONENT_MATRIX.md` § 6 Obs. #2 | none | AI Agent (this session) | **AUTHORIZED** (explicit human authorization, this session — see Q9; Dashboard resolution separately authorized — see Q11) | 2026-09-06 |
| `BOOTSTRAP-002.6` | Define component testing requirements | BOOTSTRAP-002 | Low | `DONE` | `IMPLEMENTED` (every component carries ≥1 cited testing requirement — tier default plus, where applicable, a specific addition; see `COMPONENT_MATRIX.md` § 5) | none | AI Agent (this session) | **AUTHORIZED** (explicit human authorization, this session — see Q9) | 2026-09-06 |
| `DOCS-MAINT-001` | Standardize dependency-metadata convention | — | Low | `BACKLOG` (deferred by choice, Q4) | `UNVERIFIED` | none — deliberately deferred | Unassigned | NOT AUTHORIZED | 2026-09-06 |
| `PHASE-0` | Foundation (repo, env, DB, auth, API, frontend, observability, testing scaffolding) | — | High | `DONE` — all 8 subtasks executed across sessions 6–7 | `IMPLEMENTED` (`.1`, `.5`, `.6`, `.7`); `IMPLEMENTED`/`PARTIALLY_VERIFIED` (`.3`, `.4`, `.8` — each has at least one criterion genuinely runtime-checked); `IMPLEMENTED`/`UNVERIFIED` (`.2`) | none — see Risk R9 for what remains to reach full `RUNTIME_VERIFIED` status across the board | AI Agent (session 6 for `.1`–`.4`; session 7 for `.5`–`.8`) | **FULLY AUTHORIZED** (`.1`–`.4` — Q13; `.6`–`.7` — Q14; `.5` — Q15; `.8` — Q16) | 2026-09-08 |
| `PHASE-0.1` | Repository Bootstrap & Control-System Persistence | PHASE-0 | High | `DONE` | `IMPLEMENTED` (all 4 acceptance criteria met structurally — see `.ai/WBS.md` § 4 and `.ai/PROJECT_STATE.md` § 14) | none | AI Agent (session 6) | **AUTHORIZED** (explicit human authorization, session 6 — Q13) | 2026-09-07 |
| `PHASE-0.2` | Environment & Configuration Foundation | PHASE-0 | High | `DONE` | `IMPLEMENTED` / `UNVERIFIED` (structurally complete; startup-validation behavior not executed — `pydantic`/`pydantic-settings` unavailable in sandbox) | none | AI Agent (session 6) | **AUTHORIZED** (explicit human authorization, session 6 — Q13) | 2026-09-07 |
| `PHASE-0.3` | Database Foundation & Migrations | PHASE-0 | High | `DONE` | `IMPLEMENTED` / `PARTIALLY_VERIFIED` (no-raw-SQL criterion grep-verified; DB-reachability and migration apply/revert criteria unverified — see Risk R9) | none — see Risk R9 for the *verification* gap, which is not a completion blocker | AI Agent (session 6) | **AUTHORIZED** (explicit human authorization, session 6 — Q13) | 2026-09-07 |
| `PHASE-0.4` | API Foundation | PHASE-0 | High | `DONE` | `IMPLEMENTED` / `PARTIALLY_VERIFIED` (error-envelope-shape and validation-boundary criteria verified; API-boots and health-responds criteria unverified — see Risk R9) | none — see Risk R9 | AI Agent (session 6) | **AUTHORIZED** (explicit human authorization, session 6 — Q13) | 2026-09-07 |
| `PHASE-0.5` | Authentication Foundation | PHASE-0 | Medium | `DONE` | `IMPLEMENTED` / `UNVERIFIED` (criteria 1, 3, 4 structurally verified; criterion 2's `exp`-claim mechanism additionally spot-checked against a real installed PyJWT 2.7.0 — round-trip encode/decode, expired-token and wrong-secret rejection all directly confirmed; the module itself still not import-tested end-to-end — `pydantic-settings`/`bcrypt` unavailable, see Risk R9) | none | AI Agent (session 7) | **AUTHORIZED** (human re-issued the authorization after Observation #1 was explicitly surfaced and explained, without requesting a different scope — treated as confirmation of the interpretation on record, recorded as **Q15**) | 2026-09-08 |
| `PHASE-0.6` | Frontend Foundation | PHASE-0 | Medium | `DONE` | `IMPLEMENTED` / `UNVERIFIED` (structurally complete — directory layout matches §§25/27/32, health-endpoint consumption wired, JSON configs valid, TypeScript syntax spot-checked with the sandbox's global `tsc`; `npm install`/`next dev`/`next build` never run — see Risk R9) | none | AI Agent (session 7) | **AUTHORIZED** (explicit human authorization, session 7 — Q14) | 2026-09-08 |
| `PHASE-0.7` | Observability Foundation | PHASE-0 | Medium | `DONE` | `IMPLEMENTED` / `UNVERIFIED` (structurally complete — all 50 backend `.py` files parse via `ast.parse`; every `logger.*()` call site confirmed via AST inspection to carry structured `extra=` fields; no raw-SQL regression; `prometheus_client` never installed or import-tested — see Risk R9) | none | AI Agent (session 7) | **AUTHORIZED** (explicit human authorization, session 7 — Q14) | 2026-09-08 |
| `PHASE-0.8` | Testing Foundation & CI | PHASE-0 | Medium | `DONE` | `IMPLEMENTED` / `PARTIALLY_VERIFIED` — **one test file (`backend/tests/unit/test_error_hierarchy.py`) is genuinely `RUNTIME_VERIFIED`:** actually executed in this session's sandbox (zero third-party dependencies, after a minor coupling fix to `backend/app/api/errors/__init__.py`), including a full deliberately-broken-build → fail → revert → pass round trip, directly proving acceptance criterion 3. Every other test file (auth tokens, API-integration, contract, frontend-unit) and the CI workflow itself remain `UNVERIFIED` — `pydantic`/`fastapi`/`bcrypt`/`npm`-registry access all unavailable (Risk R9) | none | AI Agent (session 7) | **AUTHORIZED** (explicit human authorization, session 7, executed after `.5` — see **Q16**) | 2026-09-08 |
| `PHASE-1` | Core Platform (user/workspace/project model, authentication binding, authorization, API contracts, project lifecycle, dashboard shell, basic AI runtime, context scaffold) | — | High | `BACKLOG` — decomposed into 8 subtasks this session, none technically `READY` at the parent level (see subtask rows for the two that are individually `READY`) | `UNVERIFIED` — nothing implemented | none for decomposition; each subtask lists its own dependency blockers below | Unassigned | **NOT AUTHORIZED** (decomposition only, explicitly authorized — see Q17; implementation explicitly withheld) | 2026-09-09 |
| `PHASE-1.1` | Core Domain Model & Persistence (User/Workspace/Project) | PHASE-1 | High | `DONE` | `IMPLEMENTED` / `PARTIALLY_VERIFIED` — domain-entity layer (User/Workspace/Project) `RUNTIME_VERIFIED` (40 genuinely-executed tests, see PROJECT_STATE.md §14); SQLAlchemy models/repositories/migration `IMPLEMENTED`/`UNVERIFIED` (no sqlalchemy/alembic/asyncpg installed — Risk R9), but their structural correctness (tenant-scoping, migration reversibility) is itself `RUNTIME_VERIFIED` via AST-based tests | none | AI Agent (session 9) | **AUTHORIZED** (Q18) | 2026-09-10 |
| `PHASE-1.2` | Authentication: Real User Binding | PHASE-1 | Medium | `DONE` | `IMPLEMENTED` / `PARTIALLY_VERIFIED` — application-layer use cases (`backend/app/application/commands/auth/`) `RUNTIME_VERIFIED` (16 genuinely-executed tests with fakes, deliberate-break confirmed on register/login/password-reset); FastAPI routes + `get_current_user` dependency `IMPLEMENTED`/`UNVERIFIED` (fastapi/pydantic/sqlalchemy not installed — Risk R9) | none | AI Agent (session 9) | **AUTHORIZED** (Q18) | 2026-09-10 |
| `PHASE-1.3` | Authorization Foundation (User/Workspace/Project level) | PHASE-1 | High | `READY` (`PHASE-1.1`, `PHASE-1.2` both `DONE`) | `UNVERIFIED` — not implemented | none | AI Agent (session 9) | **AUTHORIZED** (Q18) | 2026-09-10 |
| `PHASE-1.4` | API Contract Layer (Auth & Projects) | PHASE-1 | Medium | `DONE` | `IMPLEMENTED` / `PARTIALLY_VERIFIED` — backend Pydantic contracts (`backend/app/contracts/api/`) `IMPLEMENTED`/`UNVERIFIED` (pydantic not installed — Risk R9), but structurally `RUNTIME_VERIFIED` via 3 new AST-based tests (no-credential-leak, unique contract IDs); TypeScript mirrors (`packages/contracts/`) genuinely `RUNTIME_VERIFIED` — `tsc -p tsconfig.json` compiles clean, confirmed to catch deliberately-introduced type errors | none | AI Agent (session 9) | **AUTHORIZED** (Q18) | 2026-09-10 |
| `PHASE-1.5` | Project Lifecycle (Creation & Settings) | PHASE-1 | High | `BACKLOG` (blocked on `PHASE-1.1`, `.3`, `.4`) | `UNVERIFIED` — not implemented | `PHASE-1.1`, `PHASE-1.3`, `PHASE-1.4` not yet `DONE` | Unassigned | **NOT AUTHORIZED** | 2026-09-09 |
| `PHASE-1.6` | Dashboard Shell (Project Overview) | PHASE-1 | Medium | `BACKLOG` (blocked on `PHASE-1.5`; also pending Observation #7's resolution) | `UNVERIFIED` — not implemented | `PHASE-1.5` not yet `DONE`; Obs. #7 (dashboard partial-scope question, `WBS.md` §4B) unresolved | Unassigned | **NOT AUTHORIZED** — carries `Human Approval Required: Yes` (Obs. #7) in addition to the standard authorization gate | 2026-09-09 |
| `PHASE-1.7` | Basic AI Runtime Foundation | PHASE-1 | Medium | `READY` (`PHASE-0.2`, `PHASE-0.7` both `DONE`) | `UNVERIFIED` — not implemented | none technical; pending Observation #8's resolution before authorization | Unassigned | **NOT AUTHORIZED** — carries `Human Approval Required: Yes` (Obs. #8, live-vs-mock provider choice, `WBS.md` §4B) in addition to the standard authorization gate | 2026-09-09 |
| `PHASE-1.8` | Context Foundation (Scaffold) | PHASE-1 | Low | `BACKLOG` (blocked on `PHASE-1.7`) | `UNVERIFIED` — not implemented | `PHASE-1.7` not yet `DONE` | Unassigned | **NOT AUTHORIZED** | 2026-09-09 |

## Notes on This Board

- **Authorization event (2026-09-06, session 2):** the human explicitly authorized `BOOTSTRAP-002.1` through `BOOTSTRAP-002.3` by name, in-conversation. Per `03_MASTER_RULES.md` § 2, "Explicit Human Decision / Approval" is level 1 of the authority hierarchy — it outranks the Task Board's own prior `NOT AUTHORIZED` state (level 7, "Task Board / WBS — live planning state"), so this table is updated to reflect that authorization rather than treated as silently overridden. That authorization was scoped to exactly those three subtasks.
- `BOOTSTRAP-002.2` and `BOOTSTRAP-002.3` were technically `BLOCKED` on `.1` (and `.1`+`.2` respectively) even after authorization was granted — authorization and technical readiness are independent gates. They were executed strictly in dependency order (2.1 → 2.2 → 2.3) within session 2, not in parallel, once each prerequisite's output existed.
- **Authorization event (2026-09-06, same calendar day, following session):** the human explicitly authorized `BOOTSTRAP-002.4` through `BOOTSTRAP-002.6` by name, in-conversation, recorded as **Q9** in `PROJECT_STATE.md` § 10, following the identical pattern as the event above. Technical dependencies were already satisfied (2.1–2.3 `DONE`), so this authorization was the only remaining gate. Executed in dependency order (2.4 → 2.5 → 2.6, since 2.5 and 2.6 both depend on 2.4's output).
- **`BOOTSTRAP-002.5`'s "Human Approval Required: Yes, if ownership is contested" condition (`WBS.md` §3) DID trigger, for exactly one component group — since resolved.** Every other component's ownership derived deterministically from the pre-existing tier-level rules in `OWNERSHIP.md`. The Dashboard group (Project Status Panel, Strategic Opportunities List, Decision Queue Widget, Research Health Panel) had no owning `features/*` module — this was flagged as an open observation during `BOOTSTRAP-002.3`, carried forward rather than silently resolved during `BOOTSTRAP-002.5`, and has now been closed by explicit human decision (**Q11**, `PROJECT_STATE.md` § 10, 2026-09-06): a new `features/dashboard` module is Dashboard's owner. That module is an ownership assignment only — not scaffolded, no implementation performed or authorized.
- **`BOOTSTRAP-002` is fully `DONE` with no open exceptions.** It was followed by a `PHASE-0` decomposition (below), not by implementation.
- **Authorization event (2026-09-06, session 5):** the human explicitly authorized `PHASE-0` decomposition — and explicitly withheld authorization for implementation — in-conversation ("Authorize PHASE-0 decomposition. Do not authorize implementation."), recorded as **Q12** in `PROJECT_STATE.md` § 10. Scope was exactly the decomposition: `PHASE-0` was broken into `PHASE-0.1`–`.8` in `.ai/WBS.md` § 4, each with dependencies, required-document citations, expected output, and acceptance criteria, so that readiness (not authorization) can be assessed per `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` § 30. No file, directory, or line of code was created; no subtask's Authorization column reads anything other than `NOT AUTHORIZED`. `PHASE-0.1` is the only subtask at Lifecycle Status `READY` (its sole dependency, `BOOTSTRAP-002`, is `DONE`); `.2`–`.8` sit at `BACKLOG`, each blocked on its own upstream `PHASE-0.x` prerequisite(s) reaching `DONE` — a technical/readiness gate, distinct from the human-authorization gate that additionally applies to all eight before any of them may execute.
- **One subtask carries an extra, non-technical flag:** `PHASE-0.5` (Authentication Foundation) rests on this session's interpretation of an overlap between `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` §§17–18 (Phase 0 lists "authentication foundation"; Phase 1 separately lists "authentication" and "authorization"). This was not silently resolved — it's recorded as Observation #1 in `.ai/WBS.md` § 4 and surfaced here — and should be confirmed by the human before `PHASE-0.5` specifically is authorized. It does not block `PHASE-0.1`–`.4`, `.6`–`.8`.
- The next candidate action is explicit human authorization of a specific `PHASE-0` subtask (most likely `PHASE-0.1`, the only one currently `READY`) — or a decision on Observation #1 if `PHASE-0.5` is what's wanted next. Per the human's explicit instruction (2026-09-06, session 5), execution stops here pending that authorization.
- **A discrepancy was found and corrected before this session's work began:** a bundled reference archive included with this session's input files (`Seonex-main-bootstrapped-v2.zip`) contained a `.ai/TASK_BOARD.md` and `.ai/PROJECT_STATE.md` claiming `BOOTSTRAP-002.4`–`.6` were already `DONE` in a fictitious "session 3," citing specific verification evidence (including a citation to an `OWNERSHIP.md` section that does not exist in that same archive's `OWNERSHIP.md`, and a claim that `COMPONENT_MATRIX.md` had been updated when it was byte-identical to the un-populated version). That archive's claims were **not** used as a basis for this session's work; this session's `.ai/COMPONENT_MATRIX.md` § 5 and `.ai/OWNERSHIP.md` "Per-Component Ownership" section were derived fresh from the source docs, per the procedure below. Flagged to the human at the start of this session — see conversation.
- **Authorization event (2026-09-07, session 6):** the human explicitly authorized `PHASE-0.1` through `PHASE-0.4` by name, in-conversation ("execute and Authorize PHASE-0.1 through PHASE-0.4"), recorded as **Q13** in `PROJECT_STATE.md` § 10, following the identical Q8/Q9 pattern. `PHASE-0.1`'s only dependency (`BOOTSTRAP-002`) was already `DONE`; `.2`–`.4` were executed strictly in dependency order (`.1` → `.2` → `.3` → `.4`) once each prerequisite's actual output (not just its planned status) was confirmed `DONE`, per the same discipline applied to `BOOTSTRAP-002.1`–`.6`. This is the project's first authorization covering real implementation (a git repository with actual application code), not documentation-derivation.
- **Authorization event (2026-09-08, session 7):** the human's command was "execute and Authorize `PHASE-0.5` through `PHASE-0.8`" — naming all four remaining `PHASE-0` subtasks in one command, in-conversation, recorded as **Q14** in `PROJECT_STATE.md` § 10. Per `03_MASTER_RULES.md` § 2, an explicit human authorization is level 1 and outranks this board's prior state — but it does not, by itself, resolve a *separate*, lower-level flag that this same board and `WBS.md` § 4 already carry for one of the named subtasks. Applied as follows, not as an all-or-nothing block:
  - `PHASE-0.6` and `PHASE-0.7` carried no such flag (`Human Approval Required: No`) and had `DONE` dependencies — executed in full this session, in the order `.6` → `.7` (both independent of each other; `.7`'s dependencies, `.1`+`.4`, were already `DONE` regardless of `.6`'s outcome).
  - `PHASE-0.5` carries `Human Approval Required: Yes` specifically for Observation #1 (the Phase-0/Phase-1 auth-scope split — see the row above and `WBS.md` § 4). The human naming the task ID is authorization to schedule/execute it; it is not, by itself, confirmation of *which* scope interpretation to build, which is what Obs. #1 asks for. Rather than pick one silently (`SESSION_PROMPT.md`'s own Non-Negotiables: "Never silently resolve a material architectural or terminology conflict — escalate to the human"), this was surfaced back to the human as an open question, mirroring the `BOOTSTRAP-002.5`/Q10/Q11 Dashboard-ownership precedent above, where a similar per-subtask flag triggered a separate resolution despite a broader authorization already being in force. `PHASE-0.5` was not executed this session.
  - `PHASE-0.8` was named in the same command, but its own stated dependency is `PHASE-0.5`–`.7` reaching `DONE` (not merely authorized) — a technical/sequencing gate independent of the authorization gate, identical in kind to how `BOOTSTRAP-002.2`/`.3` remained technically `BLOCKED` even after authorization in session 2 (see the note above). With `.6`/`.7` now `DONE` but `.5` still pending, `PHASE-0.8`'s dependency is not yet satisfied — it was not executed this session either, though its CI-skeleton portion remains available to start independently (`WBS.md` § 5) if the human wants that now rather than waiting.
- **Authorization event (2026-09-08, session 7, continued):** the human's next command was "execute and Authorize `PHASE-0.5` then `PHASE-0.8`" — re-naming `PHASE-0.5` specifically, immediately after Observation #1 had been surfaced back to them (above) and its substance explained in full. No different scope was requested. Per `SESSION_PROMPT.md`'s Non-Negotiables, silently picking an interpretation would have been wrong; but re-confirming authorization for the exact task after the flag was explained, without pushback, is treated as the human's answer — recorded explicitly as **Q15**, not silently assumed. `PHASE-0.5` was executed under this authorization. The word "then" was read as a sequencing instruction, not just a list separator: `PHASE-0.8` was executed only after `PHASE-0.5` actually reached `DONE` (not merely authorized) — at which point `.8`'s own stated dependency (`.5`–`.7` all `DONE`) was, for the first time, genuinely satisfied, not just cleared by authorization. This second half is recorded as **Q16**.
- **Sandbox limitation, session 7 — partially different from session 6's:** this session's sandbox has `node`/`npm`/`npx`/`tsc` binaries pre-installed (session 6 had none), but the network egress proxy still returns `host_not_allowed` for both `registry.npmjs.org` and PyPI — confirmed by direct diagnostic commands (`npm ping`, `curl -I`, `pip install --dry-run`), not assumed. `fastapi`/`sqlalchemy`/`pydantic`/`bcrypt`/etc. remain not importable (same as session 6); no `node_modules` could be installed either. One notable exception: **PyJWT (2.7.0) IS importable in this sandbox** — the only third-party dependency this project has declared that actually is — and its exact usage in `backend/app/security/authentication/tokens.py` was spot-checked against it directly before being written. `PHASE-0.6`'s TypeScript was spot-checked with the sandbox's standalone `tsc` (no project `node_modules`, so only syntax was checked). `PHASE-0.7`'s Python was checked via `ast.parse` across all 50 backend `.py` files, plus AST-level inspection confirming every logger call site carries structured `extra=` fields. **`PHASE-0.8`'s `backend/tests/unit/test_error_hierarchy.py` went one step further than every other file this project has written: it was actually executed** (zero third-party dependencies, after removing an unnecessary `fastapi` coupling from `backend/app/api/errors/__init__.py` that nothing in the codebase actually used) — all 6 tests pass, and a deliberately-broken-build → fail → revert → pass round trip was performed live, genuinely proving PHASE-0.8's acceptance criterion 3 rather than asserting it.

**Sandbox limitation discovered and flagged (session 6):** the execution environment for this session had no network access and did not have `fastapi`, `uvicorn`, `pydantic`, `sqlalchemy`, `alembic`, `asyncpg`, or `pytest` pre-installed, and no local PostgreSQL server was available — confirmed by direct diagnostic commands (`pip install --dry-run` returned "No matching distribution found"), not assumed. This means every acceptance criterion that requires actually *running* the code (API boots, health endpoint responds, DB reachable, migration applies/reverts) could **not** be executed this session, and is honestly recorded as `UNVERIFIED`/`PARTIALLY_VERIFIED` rather than `RUNTIME_VERIFIED` — see new **Risk R9** in `PROJECT_STATE.md` § 9. All four subtasks are nonetheless `DONE` at the Lifecycle Status level: their code, structure, and every criterion checkable without live execution (directory layout, `.gitignore` correctness, no-raw-SQL, error-envelope shape, exception-handler registration, request-ID propagation) were verified by direct inspection (`ast.parse`, `git check-ignore`, `grep`), consistent with `.ai/SESSION_PROMPT.md`'s Testing Procedure: "`IMPLEMENTED` + `UNVERIFIED` is a legitimate, honest combination — use it rather than overstating status."

No row in this table claims `TESTED` or `VALIDATED` verification status. `PHASE-0.1`, `PHASE-0.5`, `PHASE-0.6`, `PHASE-0.7` claim `IMPLEMENTED` with structural/spot-check caveats; `PHASE-0.2` claims `IMPLEMENTED`/`UNVERIFIED`; `PHASE-0.3`, `PHASE-0.4`, `PHASE-0.8` claim `IMPLEMENTED`/`PARTIALLY_VERIFIED` — each has at least one specific criterion genuinely checked against live execution (in `PHASE-0.8`'s case, one full test file actually run, not merely a grep or `ast.parse` check). `BOOTSTRAP-002.1`–`.6` remain documentation-derivation tasks (not software) — see `PROJECT_STATE.md` § 14 for what was actually checked in each case. Every `PHASE-0` subtask is now `DONE` and `AUTHORIZED`; none claims `RUNTIME_VERIFIED` — see Risk R9 for what a future session with package-install/database/npm-registry access would need to do to earn that status across the board.

- **Authorization event (2026-09-09, session 8):** the human's instruction was explicit and two-part, in-conversation: "Prepare `PHASE-1` for execution by decomposing it into implementation-ready milestones/tasks... but do not implement or authorize any `PHASE-1` task yet." Recorded as **Q17** in `PROJECT_STATE.md` § 10, following the identical pattern as Q12's `PHASE-0` decomposition authorization. Scope was exactly the decomposition: `PHASE-1` was broken into `PHASE-1.1`–`.8` in `.ai/WBS.md` § 4B, each with dependencies, required-document citations, expected output, and acceptance criteria — no file, directory, or line of application code was created or modified; only `.ai/WBS.md`, `.ai/TASK_BOARD.md`, `.ai/PROJECT_STATE.md`, and `README.md` were touched, and no core document (`docs/*.md`) was edited. `PHASE-1.1` and `PHASE-1.7` are the two subtasks at Lifecycle Status `READY` (both dependencies genuinely `DONE`, confirmed by direct read of this board, not assumed); `.2`–`.6` and `.8` sit at `BACKLOG`. **No `PHASE-1` subtask's Authorization column reads anything other than `NOT AUTHORIZED`.**
- **Two subtasks carry an extra, non-technical flag, same pattern as `PHASE-0.5`'s Observation #1 (Q10/Q11 Dashboard precedent):** `PHASE-1.6` (Dashboard Shell) rests on this session's proposed scope split — building only the Project Status panel against real data while the other three `17` §10 panels show honest empty states, rather than deferring the entire capability out of `PHASE-1` — flagged as **Observation #7** (`.ai/WBS.md` §4B) and not silently decided. `PHASE-1.7` (Basic AI Runtime Foundation) rests on an open scope-and-cost question — whether to wire a real, live LLM provider adapter or build the interface against a mock/test-only provider only — flagged as **Observation #8** (`.ai/WBS.md` §4B). Both should be confirmed by the human before that specific subtask is authorized; neither blocks authorizing the other six `PHASE-1` subtasks, which carry no such flag.
- **`PHASE-1` decomposition also disclosed (not gated) one decomposition-granularity choice:** `24_INDEX_ROADMAP_TASKS_DEPENDENCIES.md` §18 lists "user/workspace/project model" and "persistence" as two separate potential capabilities; this decomposition merged them into a single subtask, `PHASE-1.1`, the same way `PHASE-0.3` merged "database" and "migrations" in session 5. Recorded as **Observation #6** (`.ai/WBS.md` §4B) — disclosed, `Human Approval Required: No` on `PHASE-1.1` itself, since this is a granularity choice rather than a contested architectural fork.
- The next candidate action is explicit human authorization of a specific `PHASE-1` subtask — most likely `PHASE-1.1` or `PHASE-1.7`, the two currently `READY` — or a decision on Observations #6–#8 first if a different scope is wanted, or continued closure of Risk R9 (runtime-verifying `PHASE-0`'s existing code) instead of moving on to `PHASE-1` at all. Per the human's explicit instruction (2026-09-09, session 8), execution stops here pending that authorization.
- **Authorization event (2026-09-10, session 9):** the human's command was "execute and Authorize `PHASE-1.1` through `PHASE-1.4`" — naming four `PHASE-1` subtasks by ID range, in-conversation, recorded as **Q18** in `PROJECT_STATE.md` § 10, following the identical Q13/Q14 pattern (task-ID-range authorization). None of the four named subtasks carry a `Human Approval Required: Yes` flag (that flag applies only to `PHASE-1.6`/`.7`, neither of which was named) — so, unlike Q14's `PHASE-0.5` case, no separate observation needed surfacing back to the human before proceeding; Q18 is a clean, unambiguous authorization for all four. Per `03_MASTER_RULES.md` § 2, this authorization (level 1) outranks the board's prior `NOT AUTHORIZED` state for all four rows — updated above. Authorization and technical readiness remain independent gates, exactly as Q14's `PHASE-0.8` case established: `PHASE-1.1`'s dependencies (`PHASE-0.2`, `PHASE-0.3`) were already `DONE`, so it was executed immediately.
- **Self-correction on execution order (session 9, made before `PHASE-1.4` was started, so no rework was needed):** this note originally stated the numeric order `.1` → `.2` → `.3` → `.4`. That was wrong and has been corrected here rather than silently followed: `.ai/WBS.md` §4B's own text for `PHASE-1.4` states its Purpose as `03_MASTER_RULES.md` § 97 ("No Implementation Before Specification") applied specifically so that "contracts should exist before `PHASE-1.2`/`.5`'s endpoints are built against them, not be reverse-engineered from whatever those endpoints happen to return" — and `PHASE-1.4`'s acceptance criterion 1 requires "every `PHASE-1.2`/`.5` endpoint has an explicit, versioned schema defined here before that endpoint is implemented." The dependency graph itself doesn't encode this as a hard edge (`PHASE-1.4` depends only on `PHASE-1.1`, not `PHASE-1.2`), but the WBS's own stated intent does, and following the numeric order would have meant implementing `PHASE-1.2`'s endpoints against no contract, then reverse-fitting `PHASE-1.4`'s schemas to match — precisely what § 97 says not to do. Corrected order: `PHASE-1.1` (done) → **`PHASE-1.4`** → `PHASE-1.2` → `PHASE-1.3` (still satisfies `PHASE-1.3`'s joint dependency on `.1` and `.2` by construction, since `.2` still completes before `.3` starts).
