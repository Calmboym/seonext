# prompts/

Agent, workflow, and system prompt templates. See `docs/20_PROJECT_STRUCTURE.md` §3, §31 (Prompt Management).

Empty at `PHASE-0` by design — no agents or workflows exist yet (`backend/app/intelligence/` and `backend/app/orchestration/` are still empty). Populated once an AI runtime and at least one agent are authorized in a later phase.
