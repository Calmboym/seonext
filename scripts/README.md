# scripts/

Developer/operational utility scripts. See `docs/20_PROJECT_STRUCTURE.md` §40.

Currently contains no scripts beyond what individual `PHASE-0.x` subtasks placed under `infrastructure/*/scripts/`. A repo-wide script (e.g. a one-command local bootstrap) is a reasonable `PHASE-0.8` or later addition, not created speculatively here.
