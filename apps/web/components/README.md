# apps/web/components/

Global, cross-feature React components not belonging to `packages/ui` (shared design-system, `docs/20_PROJECT_STRUCTURE.md` §27) or any single `features/*` module. See `docs/20_PROJECT_STRUCTURE.md` §25.

Empty at `PHASE-0.6` — no component exists yet; creating one now would be premature (this subtask's own acceptance criterion 4: "no premature component implementation", `.ai/WBS.md` §4). Populated once a later phase needs a component here specifically — not shared enough for `packages/ui`, not specific enough for one `features/*` module.
