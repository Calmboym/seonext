# apps/web/hooks/

Cross-feature React hooks not specific to any single `features/*` module (feature-local hooks live in `features/<name>/hooks/` instead, per `docs/20_PROJECT_STRUCTURE.md` §26).

Empty at `PHASE-0.6` — no shared hook exists yet.
