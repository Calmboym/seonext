# infrastructure/database/seeds/

Not yet populated. Seed data belongs to domain entities, none of which exist yet at `PHASE-0` (see `backend/app/domain/` and `backend/app/knowledge/`, both empty). Will be populated once a later phase introduces the first entities to seed.
