# infrastructure/database/fixtures/

Not yet populated. Test fixtures for domain data belong to the phase that introduces the relevant entities, and their wiring into a test runner is `PHASE-0.8` (Testing Foundation & CI) scope, not yet authorized.
